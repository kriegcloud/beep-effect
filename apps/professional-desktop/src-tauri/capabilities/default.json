{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the professional desktop shell. The bundled rpc sidecar is spawned from the Rust backend (ShellExt::sidecar in lib.rs), which runs with backend privileges and needs no frontend shell capability; this set therefore mirrors the effect-lexical-chat POC (core:default only).",
  "windows": ["main"],
  "permissions": ["core:default"]
}

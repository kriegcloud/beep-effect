/**
 * Public entry points for the codedank web app package.
 *
 * @since 0.0.0
 * @module
 */

/**
 * Package version for the codedank web app.
 *
 * @example
 * ```ts
 * import { VERSION } from "@beep/codedank-web"
 *
 * void VERSION
 * ```
 *
 * @since 0.0.0
 * @category models
 */
export const VERSION = "0.0.0" as const;

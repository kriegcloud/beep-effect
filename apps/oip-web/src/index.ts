/**
 * Public entry points for the oip web app package.
 *
 * @since 0.0.0
 * @packageDocumentation
 */

/**
 * Package version for the oip web app.
 *
 * @example
 * ```ts
 * import { VERSION } from "@beep/oip-web"
 *
 * console.log(VERSION)
 * ```
 *
 * @since 0.0.0
 * @category models
 */
export const VERSION = "0.0.0" as const;

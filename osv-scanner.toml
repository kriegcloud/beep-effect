# OSV Scanner configuration shared by the GitHub security workflow and local
# repo sanity checks.
#
# Keep this file present even when there are no accepted vulnerability ignores:
# the workflow passes it explicitly with `--config=osv-scanner.toml`, and the
# local Bun audit wrapper mirrors ignored vulnerability ids found here.
#
# Add ignored vulnerabilities only after review, with a reason and expiry date.

[[IgnoredVulns]]
id = "GHSA-gv7w-rqvm-qjhr"
ignoreUntil = 2026-09-12T00:00:00Z
reason = "esbuild 0.25.x binary-integrity advisory is Deno-module specific (RCE via NPM_CONFIG_REGISTRY when fetched as a Deno module); esbuild here is a build-time transitive dep of drizzle-kit (^0.25.10) and storybook (<=^0.27), neither of which accepts the fixed 0.28.1 yet. Revisit when dependents widen their ranges."

[[IgnoredVulns]]
id = "GHSA-hmw2-7cc7-3qxx"
ignoreUntil = 2026-09-13T00:00:00Z
reason = "form-data <4.0.6 predictable multipart boundary advisory. form-data is an HTTP-client transitive dependency, not a direct dependency of any workspace package; the fixed 4.0.6 is a patch bump pending a repo-wide lockfile remediation. Newly published advisory, not introduced by the current PR (identical versions present on main). Revisit and bump transitive form-data to >=4.0.6."

[[IgnoredVulns]]
id = "GHSA-wcpc-wj8m-hjx6"
ignoreUntil = 2026-09-13T00:00:00Z
reason = "protobufjs 7.5.8 advisory; fixed in 7.6.1. protobufjs is a transitive dependency (gRPC/Google client stack), not a direct workspace dependency. Newly published advisory, not introduced by the current PR (identical versions present on main); tracked for a repo-wide bump to >=7.6.3. Revisit when dependents widen their ranges."

[[IgnoredVulns]]
id = "GHSA-f38q-mgvj-vph7"
ignoreUntil = 2026-09-13T00:00:00Z
reason = "protobufjs 7.5.8 advisory; fixed in 7.6.3. protobufjs is a transitive dependency (gRPC/Google client stack), not a direct workspace dependency. Newly published advisory, not introduced by the current PR (identical versions present on main); tracked for a repo-wide bump to >=7.6.3. Revisit when dependents widen their ranges."

[[IgnoredVulns]]
id = "GHSA-h67p-54hq-rp68"
ignoreUntil = 2026-09-13T00:00:00Z
reason = "js-yaml advisory affecting 3.14.2 and 4.1.1; fixed in 4.2.0. js-yaml is a transitive dependency (the 3.14.2 line is pulled by a legacy dependent that has not adopted the 4.x major). Newly published advisory, not introduced by the current PR (identical versions present on main); tracked for a repo-wide bump to >=4.2.0. Revisit when dependents widen their ranges."

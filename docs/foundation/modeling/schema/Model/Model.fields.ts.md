---
title: Model.fields.ts
nav_order: 153
parent: "@beep/schema"
---

## Model.fields.ts overview

Internal schema module support.

Since v0.0.0

---
## Exports Grouped by Category
- [constructors](#constructors)
  - [FieldOption](#fieldoption)
  - [Generated](#generated)
  - [GeneratedByApp](#generatedbyapp)
  - [Sensitive](#sensitive)
  - [optionalOption](#optionaloption)
- [models](#models)
  - [FieldOption (interface)](#fieldoption-interface)
  - [Generated (interface)](#generated-interface)
  - [GeneratedByApp (interface)](#generatedbyapp-interface)
  - [Sensitive (interface)](#sensitive-interface)
  - [optionalOption (interface)](#optionaloption-interface)
---

# constructors

## FieldOption

Convert a field to one that is optional for all variants.

For the database variants, it will accept `null`able values.
For the JSON variants, it will also accept missing keys.

**Example**

```ts
import * as Schema from "effect/Schema"
import * as Model from "@beep/schema/Model"

const opt = Model.FieldOption(Schema.String)
console.log(opt)
```

**Signature**

```ts
declare const FieldOption: <Field extends VariantSchema.Field<TUnsafe.Any> | S.Top>(self: Field) => Field extends S.Top ? FieldOption<Field> : Field extends VariantSchema.Field<infer S> ? VariantSchema.Field<{ readonly [K in keyof S]: S[K] extends S.Top ? K extends VariantsDatabase ? S.OptionFromNullOr<S[K]> : optionalOption<S[K]> : never; }> : never
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L229)

Since v0.0.0

## Generated

A field that represents a column generated by the database.

Available for selection, update, and json, but omitted from insertion.

**Example**

```ts
import * as S from "effect/Schema"
import * as Model from "@beep/schema/Model"

const GroupId = S.Finite.pipe(S.brand("GroupId"))

class Group extends Model.Class<Group>("Group")({}) {}

console.log(Group)
```

**Signature**

```ts
declare const Generated: <S extends S.Top>(schema: S) => Generated<S>
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L49)

Since v0.0.0

## GeneratedByApp

A field generated by the application at runtime.

Present in all database variants and `json`, but absent from `jsonCreate`
and `jsonUpdate` because the server assigns the value.

**Example**

```ts
import * as S from "effect/Schema"
import * as Model from "@beep/schema/Model"

const field = Model.GeneratedByApp(S.String)
console.log(field.schemas.select.ast._tag)
```

**Signature**

```ts
declare const GeneratedByApp: <S extends S.Top>(schema: S) => GeneratedByApp<S>
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L88)

Since v0.0.0

## Sensitive

A field for sensitive values that must never appear in JSON API responses.

Present in `select`, `insert`, and `update` only.

**Example**

```ts
import * as S from "effect/Schema"
import * as Model from "@beep/schema/Model"

class User extends Model.Class<User>("User")({}) {}

console.log(User)
```

**Signature**

```ts
declare const Sensitive: <S extends S.Top>(schema: S) => Sensitive<S>
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L127)

Since v0.0.0

## optionalOption

Build a schema that decodes an optional nullable JSON key into an `Option`.

**Example**

```ts
import * as Schema from "effect/Schema"
import * as Model from "@beep/schema/Model"

const opt = Model.optionalOption(Schema.Finite)
console.log(opt)
```

**Signature**

```ts
declare const optionalOption: <S extends S.Top>(schema: S) => optionalOption<S>
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L169)

Since v0.0.0

# models

## FieldOption (interface)

Convert a field to one that is optional for all variants.

For the database variants, it will accept `null`able values.
For the JSON variants, it will also accept missing keys.

**Example**

```ts
import * as Schema from "effect/Schema"
import * as Model from "@beep/schema/Model"

const opt: Model.FieldOption<typeof Schema.String> =
  Model.FieldOption(Schema.String)

console.log(opt)
```

**Signature**

```ts
export interface FieldOption<S extends S.Top>
  extends VariantSchema.Field<{
    readonly select: S.OptionFromNullOr<S>;
    readonly insert: S.OptionFromNullOr<S>;
    readonly update: S.OptionFromNullOr<S>;
    readonly json: optionalOption<S>;
    readonly jsonCreate: optionalOption<S>;
    readonly jsonUpdate: optionalOption<S>;
  }> {}
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L201)

Since v0.0.0

## Generated (interface)

Interface for a database-generated field present in `select`, `update`, and `json` variants.

**Signature**

```ts
export interface Generated<S extends S.Top>
  extends VariantSchema.Field<{
    readonly select: S;
    readonly update: S;
    readonly json: S;
  }> {}
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L22)

Since v0.0.0

## GeneratedByApp (interface)

Interface for an application-generated field present in `select`, `insert`, `update`, and `json` variants.

**Signature**

```ts
export interface GeneratedByApp<S extends S.Top>
  extends VariantSchema.Field<{
    readonly select: S;
    readonly insert: S;
    readonly update: S;
    readonly json: S;
  }> {}
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L62)

Since v0.0.0

## Sensitive (interface)

Interface for a sensitive field excluded from all JSON variants.

**Signature**

```ts
export interface Sensitive<S extends S.Top>
  extends VariantSchema.Field<{
    readonly select: S;
    readonly insert: S;
    readonly update: S;
  }> {}
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L102)

Since v0.0.0

## optionalOption (interface)

Schema that decodes an optional nullable key into an `Option`.

**Example**

```ts
import * as Schema from "effect/Schema"
import * as Model from "@beep/schema/Model"

const field: Model.optionalOption<typeof Schema.String> =
  Model.optionalOption(Schema.String)

console.log(field)
```

**Signature**

```ts
export interface optionalOption<S extends S.Top>
  extends S.decodeTo<S.Option<S.toType<S>>, S.optionalKey<S.NullOr<S>>> {}
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/schema/src/Model/Model.fields.ts#L151)

Since v0.0.0
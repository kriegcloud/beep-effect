---
title: i18n.ts
nav_order: 1
parent: "@beep/messages"
---

## i18n.ts overview

The repositories i18n configuration.

Since v0.0.0

---
## Exports Grouped by Category
- [models](#models)
  - [GetLogIssuesOptions (type alias)](#getlogissuesoptions-type-alias)
- [utilities](#utilities)
  - [getLogIssues](#getlogissues)
  - [leafHook](#leafhook)
  - [logIssues](#logissues)
  - [t](#t)
---

# models

## GetLogIssuesOptions (type alias)

Configuration for schema issue formatting hooks.

**Example**

```ts
```typescript
import type { GetLogIssuesOptions } from "@beep/messages"

const opts: GetLogIssuesOptions = {


}
console.log(opts)
```
```

**Signature**

```ts
type GetLogIssuesOptions = Readonly<{
  readonly checkHook?: undefined | SchemaIssue.CheckHook;
  readonly leafHook?: undefined | SchemaIssue.LeafHook;
}>
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/messages/src/i18n.ts#L66)

Since v0.0.0

# utilities

## getLogIssues

A helper to retrieve issues from schema parse issues

**Example**

```ts
import { getLogIssues, t } from "@beep/messages";
import * as S from "effect/Schema";

const Person = S.Struct({});

// Configure hooks to customize how issues are rendered
const logIssues = getLogIssues({})

// Invalid object (not even a struct)
logIssues(Person, null)
// Failure(Cause([Fail([{"path":[],"message":"Please enter a valid object"}])]))

// Missing "name" key
logIssues(Person, {})
// Failure(Cause([Fail([{"path":["name"],"message":"This field is required"}])]))

// "name" has the wrong type
logIssues(Person, { name: 1 })
// Failure(Cause([Fail([{"path":["name"],"message":"Please enter a valid string"}])]))

// "name" is an empty string
logIssues(Person, { name: "" })
// Failure(Cause([Fail([{"path":["name"],"message":"Please enter at least 1 character(s)"}])]))
```

**Signature**

```ts
declare const getLogIssues: (options?: GetLogIssuesOptions) => <S extends S.Codec<unknown, unknown>>(schema: S, input: unknown) => void
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/messages/src/i18n.ts#L105)

Since v0.0.0

## leafHook

Default formatter hook for leaf-level schema issues.

Maps each schema issue variant to an i18n translated message string.

**Example**

```ts
```typescript
import { leafHook } from "@beep/messages"

console.log(typeof leafHook) // "function"
```
```

**Signature**

```ts
declare const leafHook: (u: SchemaIssue.Leaf) => string
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/messages/src/i18n.ts#L142)

Since v0.0.0

## logIssues

Logs schema validation issues for i18n payloads.

**Example**

```ts
```typescript
import { logIssues } from "@beep/messages"
import * as S from "effect/Schema"

logIssues(S.Struct({ name: S.NonEmptyString }), { name: "" })
```
```

**Signature**

```ts
declare const logIssues: { <S extends S.Codec<unknown, unknown>>(schema: S, input: unknown): void; <S extends S.Codec<unknown, unknown>>(schema: S): (input: unknown) => void; }
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/messages/src/i18n.ts#L205)

Since v0.0.0

## t

Translation function configured for repository messages.

**Example**

```ts
```typescript
import { t } from "@beep/messages"

const msg = t("struct.missingKey")
console.log(msg) // "This field is required"
```
```

**Signature**

```ts
declare const t: TFunction<["translation", ...string[]], undefined>
```

[Source](https://github.com/kriegcloud/beep-effect/tree/main/packages/foundation/modeling/messages/src/i18n.ts#L47)

Since v0.0.0
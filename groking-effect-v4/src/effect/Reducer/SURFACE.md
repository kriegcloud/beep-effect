# effect/Reducer Surface

Total exports: 3

| Export | Kind | Overview |
|---|---|---|
| `flip` | `function` | No summary found in JSDoc. |
| `make` | `function` | Creates a `Reducer` from a `combine` function and an `initialValue`. |
| `Reducer` | `interface` | A `Reducer` is a `Combiner` with an `initialValue` and a way to combine a whole collection. Think `Array.prototype.reduce`, but reusable. |

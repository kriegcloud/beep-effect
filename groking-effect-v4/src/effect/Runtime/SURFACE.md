# effect/Runtime Surface

Total exports: 3

| Export | Kind | Overview |
|---|---|---|
| `defaultTeardown` | `const` | The default teardown function that determines exit codes based on Effect completion. |
| `makeRunMain` | `const` | Creates a platform-specific main program runner that handles Effect execution lifecycle. |
| `Teardown` | `interface` | Represents a teardown function that handles program completion and determines the exit code. |

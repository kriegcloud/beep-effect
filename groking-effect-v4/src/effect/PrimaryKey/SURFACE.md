# effect/PrimaryKey Surface

Total exports: 4

| Export | Kind | Overview |
|---|---|---|
| `isPrimaryKey` | `const` | No summary found in JSDoc. |
| `PrimaryKey` | `interface` | An interface for objects that can provide a string-based primary key. |
| `symbol` | `const` | The unique identifier used to identify objects that implement the `PrimaryKey` interface. |
| `value` | `const` | Extracts the string value from a `PrimaryKey`. |

# effect/MutableHashMap Surface

Total exports: 16

| Export | Kind | Overview |
|---|---|---|
| `clear` | `const` | Removes all key-value pairs from the MutableHashMap, mutating the map in place. The map becomes empty after this operation. |
| `empty` | `const` | Creates an empty MutableHashMap. |
| `forEach` | `const` | No summary found in JSDoc. |
| `fromIterable` | `const` | Creates a MutableHashMap from an iterable collection of key-value pairs. |
| `get` | `const` | Retrieves the value associated with the specified key from the MutableHashMap. |
| `has` | `const` | Checks if the MutableHashMap contains the specified key. |
| `isEmpty` | `const` | No summary found in JSDoc. |
| `keys` | `const` | Extracts all keys from the MutableHashMap into an array. |
| `make` | `const` | Creates a MutableHashMap from a variable number of key-value pairs. |
| `modify` | `const` | Updates the value of the specified key within the MutableHashMap if it exists. If the key doesn't exist, the map remains unchanged. |
| `modifyAt` | `const` | Sets or removes the specified key in the MutableHashMap using an update function. The function receives the current value as an Option and returns an Option. If the function ret... |
| `MutableHashMap` | `interface` | No summary found in JSDoc. |
| `remove` | `const` | Removes the specified key from the MutableHashMap, mutating the map in place. If the key doesn't exist, the map remains unchanged. |
| `set` | `const` | Sets a key-value pair in the MutableHashMap, mutating the map in place. If the key already exists, its value is updated. |
| `size` | `const` | Returns the number of key-value pairs in the MutableHashMap. |
| `values` | `const` | Extracts all values from the MutableHashMap into an array. |

# effect/Random Surface

Total exports: 7

| Export | Kind | Overview |
|---|---|---|
| `next` | `const` | Generates a random number between 0 (inclusive) and 1 (inclusive). |
| `nextBetween` | `const` | Generates a random number between `min` (inclusive) and `max` (inclusive). |
| `nextInt` | `const` | Generates a random integer between `Number.MIN_SAFE_INTEGER` (inclusive) and `Number.MAX_SAFE_INTEGER` (inclusive). |
| `nextIntBetween` | `const` | Generates a random number between `min` (inclusive) and `max` (inclusive). |
| `nextUUIDv4` | `const` | Generates a random UUID (v4) string. |
| `Random` | `const` | Represents a service for generating random numbers. |
| `withSeed` | `const` | Seeds the pseudorandom number generator with the specified value. |

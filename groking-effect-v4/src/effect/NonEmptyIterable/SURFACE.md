# effect/NonEmptyIterable Surface

Total exports: 3

| Export | Kind | Overview |
|---|---|---|
| `nonEmpty` | `const` | A unique symbol used to brand the `NonEmptyIterable` type. |
| `NonEmptyIterable` | `interface` | Represents an iterable that is guaranteed to contain at least one element. |
| `unprepend` | `const` | Safely extracts the first element and remaining elements from a non-empty iterable. |

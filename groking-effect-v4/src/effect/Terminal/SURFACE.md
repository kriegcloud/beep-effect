# effect/Terminal Surface

Total exports: 6

| Export | Kind | Overview |
|---|---|---|
| `isQuitError` | `const` | No summary found in JSDoc. |
| `Key` | `interface` | No summary found in JSDoc. |
| `make` | `const` | Creates a Terminal implementation |
| `QuitError` | `class` | A `QuitError` represents an error that occurs when a user attempts to quit out of a `Terminal` prompt for input (usually by entering `ctrl`+`c`). |
| `Terminal` | `interface` | A `Terminal` represents a command-line interface which can read input from a user and display messages to a user. |
| `UserInput` | `interface` | No summary found in JSDoc. |

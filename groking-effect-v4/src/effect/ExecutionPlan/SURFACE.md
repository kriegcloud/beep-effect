# effect/ExecutionPlan Surface

Total exports: 8

| Export | Kind | Overview |
|---|---|---|
| `ConfigBase` | `type` | No summary found in JSDoc. |
| `CurrentMetadata` | `const` | No summary found in JSDoc. |
| `ExecutionPlan` | `interface` | A `ExecutionPlan` can be used with `Effect.withExecutionPlan` or `Stream.withExecutionPlan`, allowing you to provide different resources for each step of execution until the eff... |
| `isExecutionPlan` | `const` | No summary found in JSDoc. |
| `make` | `const` | Create an `ExecutionPlan`, which can be used with `Effect.withExecutionPlan` or `Stream.withExecutionPlan`, allowing you to provide different resources for each step of executio... |
| `merge` | `const` | No summary found in JSDoc. |
| `Metadata` | `interface` | No summary found in JSDoc. |
| `TypeId` | `type` | No summary found in JSDoc. |

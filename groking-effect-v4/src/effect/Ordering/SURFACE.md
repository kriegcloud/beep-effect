# effect/Ordering Surface

Total exports: 4

| Export | Kind | Overview |
|---|---|---|
| `match` | `const` | Depending on the `Ordering` parameter given to it, returns a value produced by one of the 3 functions provided as parameters. |
| `Ordering` | `type` | Represents the result of comparing two values. |
| `Reducer` | `const` | A `Reducer` for combining `Ordering`s. |
| `reverse` | `const` | Inverts the ordering of the input Ordering. This is useful for creating descending sort orders from ascending ones. |

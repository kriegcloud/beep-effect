# effect/unstable/cluster/EntityResource Surface

Total exports: 5

| Export | Kind | Overview |
|---|---|---|
| `CloseScope` | `class` | A `Scope` that is only closed when the resource is explicitly closed. |
| `EntityResource` | `interface` | No summary found in JSDoc. |
| `make` | `const` | A `EntityResource` is a resource that can be acquired inside a cluster entity, which will keep the entity alive even across restarts. |
| `makeK8sPod` | `const` | No summary found in JSDoc. |
| `TypeId` | `const` | No summary found in JSDoc. |

# effect/HashSet Surface

Total exports: 19

| Export | Kind | Overview |
|---|---|---|
| `add` | `const` | Adds a value to the HashSet, returning a new HashSet. |
| `difference` | `const` | Creates the difference of two HashSets (elements in the first set that are not in the second). |
| `empty` | `const` | Creates an empty HashSet. |
| `every` | `const` | Tests whether all values in the HashSet satisfy the predicate. |
| `filter` | `const` | Filters the HashSet keeping only values that satisfy the predicate. |
| `fromIterable` | `const` | Creates a HashSet from an iterable collection of values. |
| `has` | `const` | Checks if the HashSet contains the specified value. |
| `HashSet` | `interface` | A HashSet is an immutable set data structure that provides efficient storage and retrieval of unique values. It uses a HashMap internally for optimal performance. |
| `intersection` | `const` | Creates the intersection of two HashSets. |
| `isEmpty` | `const` | Checks if the HashSet is empty. |
| `isHashSet` | `const` | Checks if a value is a HashSet. |
| `isSubset` | `const` | Checks if a HashSet is a subset of another HashSet. |
| `make` | `const` | Creates a HashSet from a variable number of values. |
| `map` | `const` | Maps each value in the HashSet using the provided function. |
| `reduce` | `const` | Reduces the HashSet to a single value by iterating through the values and applying an accumulator function. |
| `remove` | `const` | Removes a value from the HashSet, returning a new HashSet. |
| `size` | `const` | Returns the number of values in the HashSet. |
| `some` | `const` | Tests whether at least one value in the HashSet satisfies the predicate. |
| `union` | `const` | Creates the union of two HashSets. |

# effect/encoding/Hex Surface

Total exports: 3

| Export | Kind | Overview |
|---|---|---|
| `decode` | `const` | Decodes a hex encoded `string` into a `Uint8Array`. |
| `decodeString` | `const` | Decodes a hex encoded `string` into a UTF-8 `string`. |
| `encode` | `const` | Encodes the given value into a hex `string`. |

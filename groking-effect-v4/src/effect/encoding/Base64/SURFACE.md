# effect/Encoding Surface

Total exports: 3

| Export | Kind | Overview |
|---|---|---|
| `decode` | `const` | Decodes a base64 (RFC4648) encoded `string` into a `Uint8Array`. |
| `decodeString` | `const` | Decodes a base64 (RFC4648) encoded `string` into a UTF-8 `string`. |
| `encode` | `const` | Encodes the given value into a base64 (RFC4648) `string`. |

# effect/Redactable Surface

Total exports: 6

| Export | Kind | Overview |
|---|---|---|
| `currentFiberTypeId` | `const` | No summary found in JSDoc. |
| `getRedacted` | `function` | No summary found in JSDoc. |
| `isRedactable` | `const` | Checks if a value implements the `Redactable` interface. |
| `redact` | `function` | Applies redaction to a value if it implements the Redactable interface. |
| `Redactable` | `interface` | Interface for objects that can provide redacted representations. |
| `symbolRedactable` | `const` | Symbol used to identify objects that implement redaction capabilities. |

# effect/JsonPatch Surface

Total exports: 4

| Export | Kind | Overview |
|---|---|---|
| `apply` | `function` | Apply a JSON Patch to a document. |
| `get` | `function` | Compute a patch that transforms `oldValue` into `newValue`. |
| `JsonPatch` | `type` | A JSON Patch document (an ordered list of operations). |
| `JsonPatchOperation` | `type` | A single JSON Patch operation. |

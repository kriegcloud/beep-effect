# effect/Ref Surface

Total exports: 16

| Export | Kind | Overview |
|---|---|---|
| `get` | `const` | Gets the current value of the Ref. |
| `getAndSet` | `const` | Atomically gets the current value of the Ref and sets it to the specified value. |
| `getAndUpdate` | `const` | Atomically gets the current value of the Ref and updates it with the given function. |
| `getAndUpdateSome` | `const` | Atomically gets the current value of the Ref and updates it with the given partial function. |
| `getUnsafe` | `const` | Gets the current value of the Ref synchronously (unsafe version). |
| `make` | `const` | Creates a new Ref with the specified initial value. |
| `makeUnsafe` | `const` | Creates a new Ref with the specified initial value (unsafe version). |
| `modify` | `const` | Atomically modifies the value of the Ref using the given function. |
| `modifySome` | `const` | Atomically modifies the value of the Ref using the given partial function. |
| `Ref` | `interface` | A mutable reference that provides atomic read, write, and update operations. |
| `set` | `const` | Sets the value of the Ref to the specified value. |
| `setAndGet` | `const` | Atomically sets the value of the Ref to the specified value and returns the new value. |
| `update` | `const` | Atomically updates the value of the Ref using the given function. |
| `updateAndGet` | `const` | Atomically updates the value of the Ref using the given function and returns the new value. |
| `updateSome` | `const` | Atomically updates the value of the Ref using the given partial function. |
| `updateSomeAndGet` | `const` | Atomically updates the value of the Ref using the given partial function and returns the current value. |

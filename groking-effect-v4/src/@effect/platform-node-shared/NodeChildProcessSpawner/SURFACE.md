# @effect/platform-node-shared/NodeChildProcessSpawner Surface

Total exports: 3

| Export | Kind | Overview |
|---|---|---|
| `flattenCommand` | `const` | Flattens a `Command` into an array of `StandardCommand`s along with pipe options for each connection. |
| `FlattenedPipeline` | `interface` | Result of flattening a pipeline of commands. |
| `layer` | `const` | Layer providing the `NodeChildProcessSpawner` implementation. |

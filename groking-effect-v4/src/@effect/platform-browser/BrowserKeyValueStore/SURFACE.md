# @effect/platform-browser/BrowserKeyValueStore Surface

Total exports: 2

| Export | Kind | Overview |
|---|---|---|
| `layerLocalStorage` | `const` | Creates a `KeyValueStore` layer that uses the browser's `localStorage` api. |
| `layerSessionStorage` | `const` | Creates a `KeyValueStore` layer that uses the browser's `sessionStorage` api. |

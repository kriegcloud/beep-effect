# @effect/vitest/utils Surface

Total exports: 21

| Export | Kind | Overview |
|---|---|---|
| `assertDefined` | `function` | Asserts that `a` is not `undefined`. |
| `assertEquals` | `function` | Asserts that `actual` is equal to `expected` using the `Equal.equals` trait. |
| `assertExitFailure` | `function` | Asserts that `exit` is a failure. |
| `assertExitSuccess` | `function` | Asserts that `exit` is a success. |
| `assertFailure` | `function` | Asserts that `result` is `Failure`. |
| `assertFalse` | `function` | Asserts that `self` is `false`. |
| `assertInclude` | `function` | Asserts that `actual` includes `expected`. |
| `assertInstanceOf` | `function` | Asserts that `value` is an instance of `constructor`. |
| `assertMatch` | `function` | Asserts that `actual` matches `regExp`. |
| `assertNone` | `function` | Asserts that `option` is `None`. |
| `assertSome` | `function` | Asserts that `option` is `Some`. |
| `assertSuccess` | `function` | Asserts that `result` is `Success`. |
| `assertTrue` | `function` | Asserts that `self` is `true`. |
| `assertUndefined` | `function` | Asserts that `a` is `undefined`. |
| `deepStrictEqual` | `function` | Asserts that `actual` is equal to `expected` using the `Equal.equals` trait. |
| `doesNotThrow` | `function` | Asserts that `thunk` does not throw an error. |
| `fail` | `function` | Throws an `AssertionError` with the provided error message. |
| `notDeepStrictEqual` | `function` | Asserts that `actual` is not equal to `expected` using the `Equal.equals` trait. |
| `strictEqual` | `function` | Asserts that `actual` is equal to `expected` using the `Equal.equals` trait. |
| `throws` | `function` | Asserts that `thunk` throws an error. |
| `throwsAsync` | `function` | Asserts that `thunk` throws an error. |

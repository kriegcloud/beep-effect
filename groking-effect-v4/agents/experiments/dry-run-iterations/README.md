# Dry-Run Iteration Lab

This folder stores dry-run reports and syntheses for prompt/config tuning.

Expected layout:

- `iteration-1/`
- `iteration-2/`
- `iteration-3/`

Each iteration should include:
- one report per deployed agent
- one synthesis document
- prompt/config deltas applied before next iteration

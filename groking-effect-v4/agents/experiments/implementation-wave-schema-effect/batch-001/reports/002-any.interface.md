## Changes made
- Replaced reflection-only type-erasure probes with executable runtime companion examples for `Schema.Any`.
- Updated imports to remove unused probe helpers and use `formatUnknown` for consistent output.
- Added an example using `Schema.is(Schema.Any)` across varied unknown samples.
- Added a decode/encode round-trip example using `Schema.decodeUnknownSync` and `Schema.encodeUnknownSync`.

## Verification command + outcome
- Command: `bun run /home/elpresidank/YeeBois/projects/beep-effect2/groking-effect-v4/src/effect/Schema/exports/Any.interface.ts`
- Outcome: Passed (exit code `0`).
- Observed result: both examples completed successfully and the program finished with `Demo complete for effect/Schema.Any`.

## Notes / residual risks
- `Any` has no inline source JSDoc example in this workspace, so behavior was demonstrated via runtime companion APIs.
- The round-trip example validates pass-through behavior with a representative object payload; it does not exhaustively cover all JS value categories.

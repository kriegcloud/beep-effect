# Implementation Pilot (Core effect)

Scope: non-dry pilot for core `effect` modules only.

Targets:
- `effect/Array` -> `make.const.ts`
- `effect/Cause` -> `StackTrace.class.ts`

Each worker agent owns exactly one file and must run a Bun verification command after editing.

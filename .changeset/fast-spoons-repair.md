---
---

Intentionally version-neutral: adds the `explorations/` fuzzy-front-end
convention (docs, templates, `/explore` skill, pilot packet) with no package
source changes, so no package bumps are required.

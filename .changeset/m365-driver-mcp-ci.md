---
---

No release required: adds private Microsoft 365 driver packages (`@beep/m365` and `@beep/m365-mcp`, both `private: true`) plus goal/exploration documentation and CI/review follow-up fixes. The empty changeset frontmatter is deliberate because Changesets should not publish private workspace packages.

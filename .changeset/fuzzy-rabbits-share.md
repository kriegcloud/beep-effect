---
"@beep/editor-app": patch
---

Record the editor-app Pigment Vite compatibility fix that keeps the checked Vite config green under the current dependency set, alongside the broader reuse-discovery branch work.

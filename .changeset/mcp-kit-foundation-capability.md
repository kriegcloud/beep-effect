---
---

No release required: adds the private `@beep/mcp-kit` foundation/capability package (MCP host-construction kit: credential-keyed toolkit composition, `api_key_required` envelope, tier-gate dispatch, progressive field-tier projection, span hygiene, four-hint annotations), plus a one-line `effect` dependency fix on the private `@beep/ontology` package.json (unlisted-dependency repair, no source changes). The empty changeset frontmatter is deliberate because Changesets should not publish private workspace packages.

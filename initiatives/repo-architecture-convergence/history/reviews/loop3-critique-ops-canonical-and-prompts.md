Remaining findings: 4

1. Critical: The packet still has two competing "authoritative" ledger planes. Why it matters: the root packet and `ops/` now say live compatibility and amendment governance belongs in `ops/*`, but the history entrypoints and the checked-in history ledgers still tell operators to treat `history/ledgers/*` as the live authority. That means a careful worker can update the wrong ledger surface and still believe they followed the packet. File refs: `README.md:24-29`, `SPEC.md:35-38`, `ops/README.md:42-49`, `ops/prompt-assets/manifest-and-evidence.md:33-46`, `history/README.md:19-29,49-50`, `history/quick-start.md:13-15`, `history/ledgers/README.md:3-10`, `history/ledgers/compatibility-ledger.md:3`, `history/ledgers/amendment-register.md:3`. Recommendation: keep `ops/compatibility-ledger.md` and `ops/architecture-amendment-register.md` as the only live governance surfaces, then rewrite the history entrypoints and `history/ledgers/*` files so they are explicitly historical-only or remove them from the active flow.

2. High: The executable startup/read contract is still narrower than the authoritative worker-read contract. Why it matters: `README.md`, `PLAN.md`, and `SPEC.md` require every phase to load the root packet plus `history/quick-start.md`, and require P2 through P7 to read the three standards with a stricter reread for P7. The ops and history startup surfaces do not mirror that contract, so a worker can follow the checked-in control plane and still skip mandatory inputs. File refs: `README.md:45-52`, `PLAN.md:19-28`, `SPEC.md:92-116`, `ops/README.md:7-13`, `ops/prompts/agent-prompts.md:6-16`, `ops/handoffs/README.md:8-15`, `history/README.md:12-19`, `history/quick-start.md:7-15`, `ops/manifest.json:416-422`, `ops/manifest.json:620-629`, `ops/manifest.json:1170-1178`, `ops/handoffs/HANDOFF_P2.md:15-26`, `ops/handoffs/HANDOFF_P7.md:15-24`. Recommendation: make the required read set literal and identical across the manifest, shared prompt, handoff README, phase handoffs, and history bootstrap docs: every phase should include `README.md`, `SPEC.md`, `PLAN.md`, `ops/manifest.json`, the active handoff, and `history/quick-start.md`; P2 through P7 should also require `standards/ARCHITECTURE.md`, `standards/effect-laws-v1.md`, and `standards/effect-first-development.md`; P7 should explicitly reread the two `ops/*` ledgers immediately before scoring.

3. High: The review namespace is still internally contradictory. Why it matters: the ops surface now requires per-phase review artifacts (`pX-critique.md`, `pX-remediation.md`, `pX-rereview.md`), but the history review entrypoints still say the canonical namespace is loop-scoped only and explicitly tell workers not to create `pX-*` files. That leaves review automation and handoff behavior ambiguous. File refs: `history/reviews/README.md:20-28`, `history/README.md:31-34`, `history/quick-start.md:36-42`, `ops/README.md:37-38,57-59`, `ops/prompt-assets/review-loop.md:18-22`, `ops/manifest.json:47-56`, `ops/manifest.json:435-446`. Recommendation: document one coherent model everywhere. If both loop-wide and phase-scoped review artifacts are intentional, the history review entrypoints need to say so explicitly instead of forbidding `pX-*` files.

4. Medium: The machine-readable and cross-phase precedence rules still do not match `SPEC.md`. Why it matters: the conflict-resolution order in the control plane is what workers and automation will actually follow when instructions disagree. The current manifest and cross-phase handoff omit `standards/effect-laws.allowlist.jsonc`, omit the active handoff from the ordered precedence, and do not preserve the `SPEC.md` ordering that places packet ops/handoff surfaces ahead of design/history material. File refs: `SPEC.md:76-90`, `ops/manifest.json:14-25`, `ops/handoffs/HANDOFF_P0-P7.md:11-13`. Recommendation: copy the `SPEC.md` source-of-truth order verbatim into `ops/manifest.json` and the shared cross-phase handoff, including the allowlist, current-repo-reality qualifier, `ops/manifest.json`, and the active handoff packet before design, prompt, and history surfaces.

Scope verdict: changes needed

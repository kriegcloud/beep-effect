Remaining findings: 3

1. High: the live compatibility ledger schema still cannot carry the proof fields that the packet itself requires for cutover and final closure.
Why it matters: `design/verification-and-cutover.md` says every temporary compatibility layer needs a consumer list, deletion gate, explicit validation query, and final removal proof, and `design/compatibility-ledger.md` models those fields as expected live data. The actual live ledger template in `ops/compatibility-ledger.md` still only provides `Surface`, `Owner`, `Reason`, `Issue`, phase fields, `Proof plan`, `Removal evidence`, `Status`, and `Notes`, so a worker can fill the authoritative ledger and still omit the canonical replacement, affected consumers, deletion gate, validation query, and allowlist linkage needed to prove closeout cleanly.
File references: `initiatives/repo-architecture-convergence/design/compatibility-ledger.md:13-28`; `initiatives/repo-architecture-convergence/design/verification-and-cutover.md:42-56`; `initiatives/repo-architecture-convergence/README.md:18-22`; `initiatives/repo-architecture-convergence/SPEC.md:39-42,125-129`; `initiatives/repo-architecture-convergence/ops/compatibility-ledger.md:11-25`.
Concrete recommendation: normalize the packet onto one compatibility-ledger schema in `ops/compatibility-ledger.md`, adding explicit fields for shim kind, canonical replacement, affected consumers, deletion gate, validation query, and allowlist reference, or else reduce the design and verification requirements to match the live ledger template exactly.

2. High: `.aiassistant` is still treated as a live legacy root in the design corpus, but the binding P2 block rule still omits it from the roots that must stop being canonical before downstream slice work proceeds.
Why it matters: the routing canon, decomposition matrix, path-coupling inventory, tooling cutover plan, and P0 census all model `.aiassistant` as an active root that must be retired. The live repo still has `.aiassistant` content plus direct root consumers in repo config. But the binding `SPEC.md` P2 blocker only names `packages/common`, top-level `tooling`, `packages/runtime`, `packages/shared/providers`, `.agents`, `.claude`, and `.codex`. That means the authoritative gate can pass while `.aiassistant` remains a live canonical root, which undercuts the design packet's own routing and cutover model.
File references: `initiatives/repo-architecture-convergence/design/current-state-routing-canon.md:17-31,146-172`; `initiatives/repo-architecture-convergence/design/agent-runtime-decomposition-matrix.md:5-35,85-91`; `initiatives/repo-architecture-convergence/design/legacy-path-coupling-inventory.md:23-26,46`; `initiatives/repo-architecture-convergence/design/tooling-and-agent-cutover.md:34-56,83-93`; `initiatives/repo-architecture-convergence/history/outputs/p0-consumer-importer-census.md:23-34`; `initiatives/repo-architecture-convergence/SPEC.md:187-192`; `.aiassistant/rules/agent-instructions.md:8-38`; `.markdownlint-cli2.jsonc:18-40`; `cspell.json:59-88`.
Concrete recommendation: update the authoritative control-plane surfaces, starting with `SPEC.md`, so `.aiassistant` is explicitly part of the P2 legacy-topology blocker and the required legacy-root audit set everywhere the packet defines binding gate language.

3. Medium: the path-coupling inventory still records dated baseline hit counts without a reproducible query, and the current totals already drift from straightforward repo scans.
Why it matters: `design/legacy-path-coupling-inventory.md` presents a dated `Quick repo scan` with concrete hit counts, but it does not record the exact commands that produced them. During this review, simple `rg` scans using the file's stated excludes already returned materially higher totals for roots such as `.claude`, `.codex`, and `.aiassistant` than the table records. That makes the inventory poor as a burn-down baseline, especially because the binding packet requires exact search audits and counts for closure decisions.
File references: `initiatives/repo-architecture-convergence/design/legacy-path-coupling-inventory.md:12-33`; `initiatives/repo-architecture-convergence/SPEC.md:145-148`; `initiatives/repo-architecture-convergence/PLAN.md:78-89`; `initiatives/repo-architecture-convergence/ops/prompt-assets/verification-checks.md:18-37`.
Concrete recommendation: replace each `Quick scan hits` value with an exact reproducible command and recorded count, or relabel the table as approximate guidance only and move the authoritative baseline counts into a phase artifact that stores the actual `rg` commands.

Scope verdict: changes needed

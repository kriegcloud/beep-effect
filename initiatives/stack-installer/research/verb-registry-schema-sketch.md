# Verb Registry Schema Sketch

Status: stub.

This document will sketch the schema-first registry contracts for verbs,
credential reference parameters, validation tiers, consent text generation,
skill/instruction bundle generation, and generated registry artifacts composed
by app and tooling surfaces.

Read the root `README.md`, the active phase handoff, the relevant design docs, and the prior phase outputs before writing. Execute the active phase using the required category loop of `Research -> Plan -> Implement -> Refine -> Validate`. Write or refine only the active phase output under `outputs/`. Preserve locked defaults from earlier phases. Close blockers or log them in the `Exception Ledger` before claiming phase completion.

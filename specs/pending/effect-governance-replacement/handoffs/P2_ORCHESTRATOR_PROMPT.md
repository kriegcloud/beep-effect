Read `handoffs/HANDOFF_P2.md`, `RESEARCH.md`, `VALIDATED_OPTIONS.md`, `outputs/parity-matrix.md`, `outputs/steering-eval-corpus.md`, `outputs/candidate-scorecard.md`, `package.json`, `turbo.json`, and `.github/workflows/check.yml`. Own the P2 planning pass and write or refine `PLANNING.md`.

You must verify:

- candidates are rank ordered by explicit effectiveness criteria
- one primary path is chosen
- migration and rollback posture are explicit
- the plan clearly separates the Effect lane from the JSDoc and TSDoc lane
- P3 can execute without reopening broad strategy questions

Do not implement the plan in this phase.

Before ending the phase, update `PLANNING.md`, any scoring trackers touched in this phase, and `outputs/manifest.json` so the chosen path and routing are explicit.

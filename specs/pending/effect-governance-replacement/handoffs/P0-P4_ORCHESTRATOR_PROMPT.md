Read `specs/pending/effect-governance-replacement/outputs/manifest.json`, `README.md`, `outputs/grill-log.md`, and `handoffs/HANDOFF_P0-P4.md` first. Treat `active_phase` and `active_phase_assets` in the manifest as authoritative. Then read the active phase handoff and the matching `P*_ORCHESTRATOR_PROMPT.md`, not every phase artifact at once. Keep the Effect-specific governance lane separate from the JSDoc and TSDoc lane, keep P0 through P2 read-only outside this spec package, and remember that the allowed end states remain `full replacement`, `staged cutover`, or `no-go yet`.

Read `handoffs/HANDOFF_P0.md`, `README.md`, `outputs/manifest.json`, `outputs/grill-log.md`, the current Effect-rule implementations under `tooling/configs/src/eslint/`, the fixtures in `tooling/configs/test/eslint-rules.test.ts`, the root `package.json`, `turbo.json`, `.github/workflows/check.yml`, `.claude/hooks/README.md`, `.codex/config.toml`, `.codex/agents/README.md`, the repo-memory indexing surfaces, and `/home/elpresidank/YeeBois/dev/biome-effect-linting-rules/README.md` plus `ROADMAP.md`. Own the P0 research plan, use parallel sub-agents where helpful, and write or refine `RESEARCH.md`. Initialize or expand `outputs/parity-matrix.md`, `outputs/steering-eval-corpus.md`, and `outputs/candidate-scorecard.md`.

You must verify:

- the current Effect-specific governance surface is explicit
- candidate mechanisms are grouped by deployment surface
- default-path steering strength is distinguished from opt-in-only help
- the JSDoc and TSDoc lane remains separate from the primary target
- production code, lint behavior, and CI wiring outside this spec package remain untouched

Before ending the phase, update `RESEARCH.md`, the relevant tracker files, and `outputs/manifest.json` so the phase state and routing are explicit.

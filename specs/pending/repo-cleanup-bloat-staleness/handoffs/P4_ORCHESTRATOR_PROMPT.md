Read `handoffs/HANDOFF_P4.md` and the repo inputs named there. Build or refine the ranked candidate inventory in `outputs/p4-ranked-candidate-inventory.md`. Present one candidate at a time. Do not delete a candidate in this orchestrator session. When the user answers `yes`, update `outputs/cleanup-checklist.md` and `outputs/manifest.json`, then hand the approved candidate to a fresh executor session using `prompts/CANDIDATE_EXECUTOR_PROMPT.md`. After the executor finishes, record verification and commit evidence, then stop for confirmation before moving to the next candidate. If the user ends the loop early, record that explicitly before closing P4.

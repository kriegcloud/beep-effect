Read `handoffs/HANDOFF_P3.md` and the repo inputs named there. Prune only the dependency, security, and platform drift justified by prior cleanup, log anything broader for P4 instead of deleting it opportunistically, write or refine `outputs/p3-dependency-security-and-platform-pruning.md`, update `outputs/cleanup-checklist.md` and `outputs/manifest.json`, summarize verification including `audit:high` when relevant, make and record the phase commit if changes were made, and stop when the P3 exit gate is met.

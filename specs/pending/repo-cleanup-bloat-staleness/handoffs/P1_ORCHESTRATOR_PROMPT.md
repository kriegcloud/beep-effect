Read `handoffs/HANDOFF_P1.md` and the repo inputs named there. Execute only the targeted workspace-removal phase, remove active references, preserve historical evidence by default, log out-of-phase findings instead of deleting them opportunistically, regenerate managed artifacts when required, write or refine `outputs/p1-workspace-removal-and-regeneration.md`, update `outputs/cleanup-checklist.md` and `outputs/manifest.json`, summarize verification, make and record the phase commit if changes were made, and stop when the P1 exit gate is met.

Read `handoffs/HANDOFF_P2.md` and the repo inputs named there. Prove what currently drives docgen, remove only genuine stale docgen assumptions or artifacts, log out-of-phase findings instead of deleting them opportunistically, write or refine `outputs/p2-docgen-verification-and-cleanup.md`, update `outputs/cleanup-checklist.md` and `outputs/manifest.json`, summarize verification, make and record the phase commit if changes were made, and stop when the P2 exit gate is met.

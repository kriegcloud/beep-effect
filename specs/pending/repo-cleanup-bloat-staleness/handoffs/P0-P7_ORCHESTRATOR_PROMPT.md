Read `AGENTS.md`, `README.md`, `outputs/manifest.json`, and `handoffs/HANDOFF_P0-P7.md`. Determine the active phase from `outputs/manifest.json`. Then read only the matching handoff and orchestrator prompt for that phase, execute that phase, update the named output artifact plus the shared trackers and `outputs/manifest.json`, and stop at the phase exit gate. Do not silently widen scope into the next phase. Preserve historical evidence by default, treat managed repo commands as part of cleanup completeness, follow the default commit cadence unless P0 overrides it, and never push or merge without explicit user confirmation.

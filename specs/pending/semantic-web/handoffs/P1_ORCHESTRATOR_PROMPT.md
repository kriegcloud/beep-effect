Read `handoffs/HANDOFF_P1.md`, the P0 output, `design/semantic-schema-metadata.md`, and the local `IRI` and `ProvO` seed modules. Define the public schema and value families, metadata coverage rules, and equality/canonicalization policy. If you are operating in Plan Mode, do not edit spec artifacts yet. First confirm the locked defaults and produce a decision-complete P1 plan. Only in a non-Plan execution session should you write the result to `outputs/p1-core-schema-and-value-design.md`.

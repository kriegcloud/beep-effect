Read `README.md`, `outputs/manifest.json`, and the current phase output for `@beep/semantic-web`. Work through phases P0-P4 in order unless the manifest sets a later `currentTargetPhase`. Preserve the locked defaults from `design/foundation-decisions.md`, use `bun` for repo commands, keep scope inside `specs/pending/semantic-web` unless local evidence requires nearby references, and do not write production package code while operating on the spec package. If you are operating in Plan Mode, do not edit spec artifacts yet. First confirm which defaults are already locked, resolve any residual ambiguity through non-mutating exploration, and produce a decision-complete phase plan. Only write or refine the current phase output when operating outside Plan Mode.

Read `README.md`, `outputs/manifest.json`, and the current phase output for `@beep/semantic-web`. Work through phases P0-P4 in order unless the manifest shows that a later phase is the only stale artifact. Preserve the locked defaults from `design/foundation-decisions.md`, use `bun` for repo commands, keep scope inside `specs/pending/semantic-web` unless local evidence requires nearby references, and do not write production package code while operating on the spec package.

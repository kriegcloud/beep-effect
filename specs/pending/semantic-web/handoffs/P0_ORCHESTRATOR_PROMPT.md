Read `handoffs/HANDOFF_P0.md`, `README.md`, and `design/module-topology-and-boundaries.md`. Finalize the v1 module families, the long-term `@beep/schema` boundary, the `IRI` and `ProvO` seed-asset posture, the internal `idna/` posture, the curated root export surface, and the upstream library classification. If you are operating in Plan Mode, do not edit spec artifacts yet. First confirm the locked defaults and produce a decision-complete P0 plan. Only in a non-Plan execution session should you write the result to `outputs/p0-package-topology-and-boundaries.md`.

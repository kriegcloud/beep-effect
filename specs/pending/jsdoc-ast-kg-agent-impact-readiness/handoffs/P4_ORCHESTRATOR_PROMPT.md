# P4 Orchestrator Prompt

Execute live ablation benchmark for KG strategy comparison.

Deliver:

1. `outputs/p4-ablation-benchmark.md`

Constraints:

1. Must include `baseline`, `semantic_only`, `ast_only`, `ast_jsdoc_hybrid`.
2. Use consistent task set across modes.
3. Provide gate-by-gate delta table.
4. Provide explicit evidence that Claude execution path runs through `@beep/ai-sdk`.

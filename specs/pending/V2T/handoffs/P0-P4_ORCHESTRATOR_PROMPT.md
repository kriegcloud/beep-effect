Read `README.md`, `outputs/manifest.json`, and `outputs/grill-log.md`. Determine the active phase from `outputs/manifest.json`, then read `handoffs/HANDOFF_P0-P4.md` plus only the matching phase handoff and matching phase orchestrator prompt. Use the current `apps/V2T` and `packages/VT2` seams as the default implementation boundary, execute only the active phase, update the named phase artifact and `outputs/manifest.json`, update `outputs/grill-log.md` during P0, and stop at the phase exit gate instead of rolling into the next phase.

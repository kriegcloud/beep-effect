# D03 — Conceptual Model and Terminology

## Purpose

Normalize canonical terminology and concept semantics for D01-D12.

## Primary Topic

T03 — canonical terminology and concept model.

## Audience

- Authors
- Reviewers
- Editors

## Scope Boundaries

### Must include

1. Exact reproduction of Tier-1 terms from the P2 term model.
2. Concept graph and collision-resolution references.
3. Term usage rules for D01-D12.

### Must exclude

1. Runbook operational guidance (delegated to D09).
2. Detailed audit process execution (delegated to D10).

## Canonical Glossary

This glossary is the Tier-1 terminology baseline for D01-D12. The table below is reproduced to preserve canonical meaning, ownership, and usage rules.

| Term | Canonical Meaning | Synonyms to Avoid | Owner | Usage Rule |
|---|---|---|---|---|
| corpus | The fixed 12-document reference set defined by this spec cycle (`D01`-`D12`). | knowledge base, reference base, package, dump | Domain Model Lead | Use `corpus` as the default term in all normative statements. |
| source area | One of the four locked input surfaces listed in `README.md`. | repository slice, input surface | Evidence Editor | Use only when referring to one of the four approved top-level input domains. |
| source artifact | A concrete file within a source area used as evidence input. | source file (ambiguous), document blob | Evidence Editor | Pair with a path when used in traceability statements. |
| evidence ID | Stable identifier that anchors a claim to source-backed evidence (`E-*`). | citation token, proof handle | Evidence Editor | Required for all normative claims derived from corpus sources. |
| normative claim | A statement treated as technical truth for architecture, contracts, gates, or methods. | narrative statement, descriptive note | Quality Lead | Must be evidence-linked or explicitly labeled assumption. |
| traceability link | Mapping record from source artifact to claim surface (`docId` + section + coverage type). | citation edge, reference hop | Evidence Editor | Use when describing source-to-claim coverage guarantees. |
| quality gate | Pass/fail phase promotion criterion defined in rubric language. | checklist item, milestone check | Quality Lead | Use only for gate conditions with blocking semantics. |
| primary ownership | Single authoritative document owner for a major topic. | shared ownership, co-primary | Spec Orchestrator | Exactly one primary owner is allowed per major topic. |
| taxonomy axis | Canonical classification dimension used in P2 crosswalk normalization. | category family, class bucket | Domain Model Lead | Use only for the five canonical axes in `taxonomy-crosswalk.md`. |
| blocker conflict | Conflict that prevents phase exit until resolved or downgraded with rationale. | major issue, critical mismatch | Quality Lead | Any `high` conflict left `open` is blocker-level by definition. |
| deferred conflict | Non-blocker conflict intentionally carried with owner, disposition, and downstream handling. | postponed issue, parked risk | Strategy Lead | Must include explicit carry target (for this cycle: D11 and/or D12). |

## Concept Graph

The concept graph below defines control flow between key semantic nodes:

1. `source area` contains one or more `source artifact` entries.
2. `source artifact` supports one or more `normative claim` entries via an `evidence ID`.
3. `normative claim` participates in one or more `quality gate` decisions.
4. `taxonomy axis` constrains classification language used in `normative claim` entries.
5. `primary ownership` constrains where each major topic is authored.
6. `blocker conflict` and `deferred conflict` govern unresolved contradiction handling.

## Term Decision Records

| Collision ID | Colliding Terms | Canonical Choice | Decision | Status |
|---|---|---|---|---|
| T-001 | corpus / knowledge base / reference base | corpus | Preserve `corpus` as canonical; legacy labels allowed only in parenthetical clarifications. | resolved |
| T-002 | source area / repository slice / input surface | source area | Normalize all four locked-input references to `source area`. | resolved |
| T-003 | normative claim / narrative statement | normative claim | Reserve `normative claim` for truth-bearing statements requiring evidence or assumption labeling. | resolved |
| T-004 | taxonomy axis / taxonomy category family | taxonomy axis | Use `taxonomy axis` only for the five canonical P2 crosswalk dimensions. | resolved |

## Collision Handling Rules

1. Tier-1 terms are normative and should be used verbatim in D01-D12.
2. Synonyms from the collision matrix are allowed only in parentheses when preserving source wording.
3. New Tier-1 terms require updates to the P2 term model before they are treated as normative.
4. Any unresolved terminology collision is logged as a conflict with owner, status, and disposition.

## Delegation Rules

1. D03 defines terminology and concept semantics only.
2. JSDoc semantic mechanics are delegated to D04.
3. Method internals are delegated to D06.
4. Traceability implementation details are delegated to D12.

## Normative Claims and Evidence

| Claim ID | Claim | Evidence ID(s) | Status |
|---|---|---|---|
| D03-C01 | The corpus uses explicit status labels (`implemented`, `specified`, `conceptual`) to classify technical assertions, and terminology in D01-D12 must preserve that distinction. | E-S03-001 | specified |
| D03-C02 | The certainty model is three-tier (`deterministic`, `type-system`, `llm-inferred`), and term usage must avoid collapsing these tiers into a single confidence class. | E-S02-001 | specified |
| D03-C03 | The canonical JSDoc semantic surface has 113 tag definitions and 113 canonical tag names, so terminology around tag identity must refer to one canonical set. | E-S01-001, E-S01-003 | implemented |
| D03-C04 | The known script path mismatch remains a deferred reliability concern and must be represented as unresolved governance vocabulary rather than resolved completion language. | E-S03-005 | implemented |
| D03-C05 | Non-text source artifacts are part of the corpus evidence surface and must be treated as first-class source artifact types in terminology and traceability language. | E-S04-005, E-S04-006 | implemented |

## Cross-References

- [D01](./D01.md)
- [D04](./D04.md)
- [D07](./D07.md)
- [D10](./D10.md)
- [D12](./D12.md)

## Completion Checklist

- [x] Tier-1 terms have single canonical definitions.
- [x] Conflicting synonyms are resolved or logged.
- [x] Referenced terms are used consistently in D01-D12.

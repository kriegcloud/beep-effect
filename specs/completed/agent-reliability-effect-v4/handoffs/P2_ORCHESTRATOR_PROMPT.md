# Phase 2 Orchestrator Prompt

Implement the runner execution contract:

1. Keep hybrid mode (`dry` + `live`).
2. Pin models to `gpt-5.2` and `claude-sonnet-4-6`.
3. Execute live runs through CLI JSON outputs.
4. Use disposable worktree isolation by default.
5. Produce baseline run/report artifacts.

Verify baseline A/B completion and update `outputs/p2-runner-contract.md`.

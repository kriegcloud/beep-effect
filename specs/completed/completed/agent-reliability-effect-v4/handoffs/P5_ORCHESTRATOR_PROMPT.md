# Phase 5 Orchestrator Prompt

Implement the KG closed loop:

1. Define typed failure ontology for benchmark runs.
2. Persist failed runs as structured Graphiti episodes (`beep-dev`).
3. Retrieve top corrective facts before execution using bounded packet logic.
4. Run controlled comparison between `adaptive` and `adaptive_kg`.

Publish impact report with measurable deltas.

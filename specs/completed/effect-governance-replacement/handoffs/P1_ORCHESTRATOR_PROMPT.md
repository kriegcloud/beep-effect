Read `handoffs/HANDOFF_P1.md`, `RESEARCH.md`, `outputs/parity-matrix.md`, `outputs/steering-eval-corpus.md`, `outputs/candidate-scorecard.md`, and any live repo files required to validate claims. Own the P1 validation pass, narrow the field to a credible shortlist, lock the fixed steering evaluation corpus, and write or refine `VALIDATED_OPTIONS.md`.

You must verify:

- the current Effect-specific rules are compared one by one
- rejected options are explicitly rejected rather than carried forward
- the evaluation corpus is locked and specific
- only credible candidates survive into P2
- the phase remains read-only outside this spec package

Before ending the phase, update `VALIDATED_OPTIONS.md`, the relevant tracker files, and `outputs/manifest.json` so the phase state and routing are explicit.

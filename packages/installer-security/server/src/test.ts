/**
 * installer security server test layer.
 *
 * @packageDocumentation
 * @category testing
 * @since 0.0.0
 */

import { InstallerSecurityServerLive } from "./Layer.js";

/**
 * Deterministic test layer for the installer-security slice.
 *
 * @category testing
 * @since 0.0.0
 */
export const InstallerSecurityServerTest = InstallerSecurityServerLive;

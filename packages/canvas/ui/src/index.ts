/**
 * Package entry point for `@beep/canvas-ui`.
 *
 * @packageDocumentation
 * @category models
 * @since 0.0.0
 */

/**
 * Package version for `@beep/canvas-ui`.
 *
 * @example
 * ```ts
 * import { VERSION } from "@beep/canvas-ui"
 *
 * console.log(VERSION)
 * ```
 *
 * @category models
 * @since 0.0.0
 */
export const VERSION = "0.0.0" as const;

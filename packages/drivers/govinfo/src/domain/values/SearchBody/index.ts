/**
 * `@beep/govinfo/domain/values/SearchBody` exports.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * The SearchBody value object module for the `@beep/govinfo` driver.
 *
 * @example
 * ```ts
 * import { SearchBody } from "@beep/govinfo/domain/values";
 *
 * console.log(SearchBody.make({}));
 * ```
 *
 * @category models
 * @since 0.0.0
 */
export * from "./SearchBody.model.ts";
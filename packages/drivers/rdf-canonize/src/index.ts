/**
 * Driver wrapper for RDF Dataset Canonicalization via `rdf-canonize`.
 *
 * @since 0.0.0
 * @packageDocumentation
 */

/**
 * Live canonicalization adapter backed by `rdf-canonize`.
 *
 * @since 0.0.0
 * @category adapters
 */
export * as canonicalization from "./adapters/canonicalization.ts";

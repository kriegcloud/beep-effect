/**
 * installer providers server test layer.
 *
 * @packageDocumentation
 * @category testing
 * @since 0.0.0
 */

import { InstallerProvidersServerLive } from "./Layer.js";

/**
 * Deterministic test layer for the installer-providers slice.
 *
 * @category testing
 * @since 0.0.0
 */
export const InstallerProvidersServerTest = InstallerProvidersServerLive;

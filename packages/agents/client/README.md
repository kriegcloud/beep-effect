# @beep/agents-client

Client adapter package for the `agents` slice. Hosts the desktop chat surface
atoms (`Chat.atoms`) that drive the `ChatRpcs` wire contract from
`@beep/agents-use-cases`.

Use `beep architecture add concept` or `beep architecture add role` to add
concept-qualified modules.

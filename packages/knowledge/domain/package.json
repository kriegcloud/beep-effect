{
  "name": "@beep/knowledge-domain",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Ontology-guided knowledge extraction, entity resolution, and GraphRAG context assembly for intelligent agents - Domain layer",
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "directory": "dist",
    "linkDirectory": false
  },
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json",
    "./entities": "./src/entities/index.ts",
    "./*": "./src/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:kriegcloud/beep-effect.git",
    "directory": "packages/knowledge/domain"
  },
  "scripts": {
    "build": "bun run build-esm && bun run build-cjs && bun run build-annotate",
    "dev": "tsc -b tsconfig.build.json --watch",
    "build-esm": "tsc -b tsconfig.build.json",
    "build-cjs": "babel build/esm --plugins @babel/transform-export-namespace-from --plugins @babel/transform-modules-commonjs --out-dir build/cjs --source-maps",
    "build-annotate": "babel build/esm --plugins annotate-pure-calls --out-dir build/esm --source-maps",
    "check": "tsgo --noEmit -p tsconfig.json",
    "test": "bun test",
    "coverage": "bun test --coverage",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "lint:circular": "bunx madge -c ."
  },
  "peerDependencies": {
    "@beep/schema": "workspace:^",
    "@beep/shared-domain": "workspace:^",
    "@effect/experimental": "catalog:",
    "@effect/sql": "catalog:",
    "effect": "catalog:",
    "effect-machine": "catalog:"
  },
  "devDependencies": {
    "@beep/schema": "workspace:^",
    "@beep/shared-domain": "workspace:^",
    "@effect/docgen": "catalog:",
    "@effect/experimental": "catalog:",
    "@effect/sql": "catalog:",
    "@total-typescript/ts-reset": "catalog:",
    "effect": "catalog:",
    "effect-machine": "catalog:"
  },
  "effect": {
    "generateExports": {
      "include": [
        "**/*.ts"
      ]
    },
    "generateIndex": {
      "include": [
        "**/*.ts"
      ]
    }
  }
}

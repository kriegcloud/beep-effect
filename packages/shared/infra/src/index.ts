/**
 * Re-exports repository implementations for shared domain entities.
 *
 * @example
 * ```typescript
 * import { FileRepo, FolderRepo, SharedRepos } from "@beep/shared-infra"
 *
 * // Access repository services in Effect context
 * ```
 *
 * @category exports
 * @since 0.1.0
 */
export * from "./repos";

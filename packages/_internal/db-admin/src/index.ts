/**
 * \@beep/db-admin
 *
 * @since 0.0.0
 */

/**
 * Current package version.
 *
 * @example
 * ```ts
 * import { VERSION } from "@beep/db-admin"
 *
 * console.log(VERSION)
 * ```
 *
 * @since 0.0.0
 * @category constants
 */
export const VERSION = "0.0.0" as const;

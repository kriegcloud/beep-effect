# @beep/fixture-lab-specimen-domain

Synthetic domain package for the `fixture-lab/Specimen` architecture automation slice.

This package is the canonical entity-domain exemplar for
`entities/Specimen`. It owns the schema-first `Specimen.model.ts` file and the
pure lifecycle `Specimen.policy.ts` file.

Exports:

- `@beep/fixture-lab-specimen-domain`
- `@beep/fixture-lab-specimen-domain/entities/Specimen`

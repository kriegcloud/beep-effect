# @beep/fixture-lab-specimen-server

Synthetic server package for the `fixture-lab/Specimen` architecture automation slice.

The canonical concept files are `Specimen.repo.ts` for the in-memory product
port implementation and `Specimen.layer.ts` for the concept server service.
Package-level `Layer.ts` composes the config-backed live layer.

Exports:

- `@beep/fixture-lab-specimen-server`
- `@beep/fixture-lab-specimen-server/layer`

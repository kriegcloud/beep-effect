{
  "name": "@beep/files-infra",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "The package which contains the infra layer for the files slice",
  "publishConfig": {
    "access": "restricted",
    "directory": "build"
  },
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json",
    "./*": "./src/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:kriegcloud/beep-effect.git",
    "directory": "packages/files/infra"
  },
  "scripts": {
    "build": "pnpm build-esm && pnpm build-cjs && pnpm build-annotate",
    "dev": "tsc -b tsconfig.build.json --watch",
    "build-esm": "tsc -b tsconfig.build.json",
    "build-cjs": "babel build/esm --plugins @babel/transform-export-namespace-from --plugins @babel/transform-modules-commonjs --out-dir build/cjs --source-maps",
    "build-annotate": "babel build/esm --plugins annotate-pure-calls --out-dir build/esm --source-maps",
    "check": "tsc -b tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf .tsbuildinfo node_modules coverage .next out dist build .turbo",
    "coverage": "vitest run --coverage",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "lint:circular": "pnpm madge -c ."
  },
  "dependencies": {
    "@effect/sql": "*",
    "@effect/sql-drizzle": "*",
    "@effect/sql-pg": "*",
    "@effect/experimental": "*",
    "@beep/files-services": "workspace:*",
    "@beep/files-domain": "workspace:*",
    "@beep/files-tables": "workspace:*",
    "@beep/core-db": "workspace:*",
    "@beep/core-env": "workspace:*",
    "@effect/platform-node": "*",
    "@beep/shared-tables": "workspace:*",
    "@beep/schema": "workspace:*",
    "@beep/invariant": "workspace:*",
    "@beep/utils": "workspace:*",
    "@effect-aws/s3": "*",
    "@effect-aws/client-s3": "*",
    "postgres": "*",
    "@effect/platform": "*",
    "@effect/cluster": "*",
    "@effect/workflow": "*",
    "drizzle-orm": "*",
    "effect": "*",
    "pg": "*",
    "@beep/constants": "workspace:*",
    "@beep/shared-domain": "workspace:*",
    "@beep/errors": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "*",
    "@beep/types": "workspace:*"
  },
  "effect": {
    "generateExports": {
      "include": [
        "**/*.ts"
      ]
    },
    "generateIndex": {
      "include": [
        "**/*.ts"
      ]
    }
  }
}

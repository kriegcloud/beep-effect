/**
 * Re-exports the contract error taxonomy implemented under
 * `src/internal/contract-error`. Prefer importing from this path so the public
 * API remains stable even if the internal layout changes.
 *
 * @see ./internal/contract-error
 */
export * from "./internal/contract-error";

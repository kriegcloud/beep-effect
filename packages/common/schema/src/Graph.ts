/**
 * Graph schema module.
 *
 * This file is being ported from the effect-v3 subtree. It currently exists so
 * the package typechecks while the full v4 implementation is in progress.
 *
 * @since 0.0.0
 * @module @beep/schema/Graph
 */

export {}

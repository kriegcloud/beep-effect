/**
 * A Schema representing a function with built-in validation on the Payload,
 * Success & Error Channels.
 * @since 0.0.0
 * @module @beep/schema/Fn
 */
// import {$SchemaId} from "@beep/identity";

// const $I = $SchemaId.create("Fn");

export const Fn = {}

import type { ThemeComponents } from "../types.ts";

export const listTheme: ThemeComponents = {
  MuiListItemAvatar: {
    styleOverrides: {
      root: {
        minWidth: "unset",
      },
    },
  },
};

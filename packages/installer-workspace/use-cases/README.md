# @beep/installer-workspace-use-cases

Shell-only use-cases package for the `installer-workspace` slice.

Use `beep architecture add concept` or `beep architecture add role` to add concept-qualified modules.

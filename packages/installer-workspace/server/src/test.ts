/**
 * installer workspace server test layer.
 *
 * @packageDocumentation
 * @category testing
 * @since 0.0.0
 */

import { InstallerWorkspaceServerLive } from "./Layer.js";

/**
 * Deterministic test layer for the installer-workspace slice.
 *
 * @category testing
 * @since 0.0.0
 */
export const InstallerWorkspaceServerTest = InstallerWorkspaceServerLive;

import type { ThemeComponents } from "../types.ts";

/**
 * @category themes
 * @since 0.0.0
 */
export const layoutTheme: ThemeComponents = {
  MuiStack: {
    defaultProps: {
      useFlexGap: true,
    },
  },
};

"use client";

/**
 * Re-exports Base UI's direction provider and hook for controlling text direction (LTR/RTL).
 *
 * @category components
 * @since 0.0.0
 */
export { DirectionProvider, useDirection } from "@base-ui/react/direction-provider";

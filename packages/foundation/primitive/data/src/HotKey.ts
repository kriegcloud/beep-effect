/**
 * Compatibility alias for keyboard shortcut constants.
 *
 * Prefer importing from `@beep/data/KeyboardShortcuts`; this module exists for
 * callers that still use the older hotkey-oriented name.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

export * from "./KeyboardShortcuts.ts";

/**
 * Primary schema and guard for DOM events.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
/**
 * Canonical aliases for the DOM event schema module.
 *
 * @category schemas
 * @since 0.0.0
 */
export { DOMEvent as DomEvent, DOMEvent as Schema, DOMEvent, isEvent } from "../dom/events.ts";

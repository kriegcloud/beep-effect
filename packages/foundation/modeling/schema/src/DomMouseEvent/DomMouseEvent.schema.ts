/**
 * Primary schema and guard for DOM mouse events.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
/**
 * Canonical aliases for the DOM mouse event schema module.
 *
 * @category schemas
 * @since 0.0.0
 */
export {
  DOMMouseEvent as DomMouseEvent,
  DOMMouseEvent as Schema,
  DOMMouseEvent,
  isMouseEvent,
} from "../dom/events.ts";

/**
 * Primary schema and guard for React CSS properties.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
/**
 * Canonical aliases for the DOM CSS properties schema module.
 *
 * @category schemas
 * @since 0.0.0
 */
export {
  DOMCssProperties as DomCssProperties,
  DOMCssProperties as Schema,
  DOMCssProperties,
  isCSSProperties,
} from "../dom/elements.ts";

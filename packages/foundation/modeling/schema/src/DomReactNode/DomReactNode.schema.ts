/**
 * Primary schema and guard for React nodes.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
/**
 * Canonical aliases for the DOM React node schema module.
 *
 * @category schemas
 * @since 0.0.0
 */
export {
  createDOMRefSchema,
  DOMReactNode as DomReactNode,
  DOMReactNode as Schema,
  DOMReactNode,
  isReactNode,
  isReactRef,
} from "../dom/elements.ts";

/**
 * Primary schema for HTTP protocol literals.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
/**
 * Canonical aliases for the HTTP protocol schema module.
 *
 * @category schemas
 * @since 0.0.0
 */
export { HttpProtocol as Schema, HttpProtocol } from "../http/HttpProtocol.ts";

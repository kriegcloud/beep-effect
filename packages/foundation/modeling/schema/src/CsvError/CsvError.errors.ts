/**
 * Reusable CSV error models.
 *
 * @packageDocumentation
 * @since 0.0.0
 */
/**
 * Canonical aliases for the CSV error module.
 *
 * @category aliases
 * @since 0.0.0
 */
export { CsvError as Error, CsvError, csvError as make, csvError } from "../csv/CsvError.ts";

/**
 * Source-only YAML parser seams for package tests.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * YAML parser seams used by schema tests.
 *
 * @category testing
 * @since 0.0.0
 */
export { makeParseYaml, makeParseYamlForSchema } from "../internal/yaml.ts";

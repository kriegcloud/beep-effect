/**
 * Primary schema role for the `Glob` concept module.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Public schema module export.
 *
 * @category schemas
 * @since 0.0.0
 */
export { Glob, Glob as Schema } from "../Glob.ts";

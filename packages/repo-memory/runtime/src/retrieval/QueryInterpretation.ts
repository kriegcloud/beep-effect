/**
 * Public re-export for query interpretation models.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

export {
  /**
   * Deterministic query interpretation union.
   *
   * @example
   * ```ts
   * import { QueryInterpretation } from "@beep/repo-memory-runtime"
   *
   * const schema = QueryInterpretation
   * ```
   *
   * @since 0.0.0
   * @category domain model
   */
  QueryInterpretation,
} from "../internal/QueryPreparation.js";

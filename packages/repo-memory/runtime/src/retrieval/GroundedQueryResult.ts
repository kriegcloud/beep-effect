/**
 * Public re-export for grounded query result models.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

export {
  /**
   * Deterministic grounded query result model.
   *
   * @example
   * ```ts
   * import { GroundedQueryResult } from "@beep/repo-memory-runtime"
   *
   * const schema = GroundedQueryResult
   * ```
   *
   * @since 0.0.0
   * @category domain model
   */
  GroundedQueryResult,
} from "./GroundedRetrieval.js";

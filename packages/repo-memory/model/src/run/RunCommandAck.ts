/**
 * Public run command acknowledgement model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Run command acknowledgement model re-export.
 *
 * @example
 * ```ts
 * import { RunCommandAck } from "@beep/repo-memory-model"
 *
 * const schema = RunCommandAck
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Run command acknowledgement model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunCommandAck,
} from "../internal/protocolModels.js";

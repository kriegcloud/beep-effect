/**
 * Public repo run kind model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Canonical repo-memory run kind model re-export.
 *
 * @example
 * ```ts
 * import { RepoRunKind } from "@beep/repo-memory-model"
 *
 * const schema = RepoRunKind
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Canonical repo-memory run kind model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RepoRunKind,
} from "../internal/domain.js";

/**
 * Public run event stream request model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Run event stream request model re-export.
 *
 * @example
 * ```ts
 * import { StreamRunEventsRequest } from "@beep/repo-memory-model"
 *
 * const schema = StreamRunEventsRequest
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Run event stream request model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  StreamRunEventsRequest,
} from "../internal/protocolModels.js";

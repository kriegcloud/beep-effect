/**
 * Public run identifier model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Stable run identifier model re-export.
 *
 * @example
 * ```ts
 * import { RunId } from "@beep/repo-memory-model"
 *
 * const schema = RunId
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Stable run identifier model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunId,
} from "../internal/domain.js";

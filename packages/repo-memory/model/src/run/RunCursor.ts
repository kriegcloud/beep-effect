/**
 * Public run replay cursor model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Replay cursor model for streamed runs re-export.
 *
 * @example
 * ```ts
 * import { RunCursor } from "@beep/repo-memory-model"
 *
 * const schema = RunCursor
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Replay cursor model for streamed runs.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunCursor,
} from "../internal/domain.js";

/**
 * Public run event sequence model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Monotonic run event sequence model re-export.
 *
 * @example
 * ```ts
 * import { RunEventSequence } from "@beep/repo-memory-model"
 *
 * const schema = RunEventSequence
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Monotonic run event sequence model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunEventSequence,
} from "../internal/domain.js";

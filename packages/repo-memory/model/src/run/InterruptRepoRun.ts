/**
 * Public interrupt-run request model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Interrupt-run request model re-export.
 *
 * @example
 * ```ts
 * import { InterruptRepoRunRequest } from "@beep/repo-memory-model"
 *
 * const schema = InterruptRepoRunRequest
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Interrupt-run request model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  InterruptRepoRunRequest,
} from "../internal/protocolModels.js";

/**
 * Public run acceptance acknowledgement model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Run acceptance acknowledgement model re-export.
 *
 * @example
 * ```ts
 * import { RunAcceptedAck } from "@beep/repo-memory-model"
 *
 * const schema = RunAcceptedAck
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Run acceptance acknowledgement model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunAcceptedAck,
} from "../internal/protocolModels.js";

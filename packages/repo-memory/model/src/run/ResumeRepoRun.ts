/**
 * Public resume-run request model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Resume-run request model re-export.
 *
 * @example
 * ```ts
 * import { ResumeRepoRunRequest } from "@beep/repo-memory-model"
 *
 * const schema = ResumeRepoRunRequest
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Resume-run request model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  ResumeRepoRunRequest,
} from "../internal/protocolModels.js";

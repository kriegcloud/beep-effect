/**
 * Public run command model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Canonical repo-memory run command model re-export.
 *
 * @example
 * ```ts
 * import { RunCommand } from "@beep/repo-memory-model"
 *
 * const schema = RunCommand
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Canonical repo-memory run command model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunCommand,
} from "../internal/domain.js";

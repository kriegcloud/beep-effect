/**
 * Public run stream failure model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Stream failure model for run subscriptions re-export.
 *
 * @example
 * ```ts
 * import { RunStreamFailure } from "@beep/repo-memory-model"
 *
 * const schema = RunStreamFailure
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Stream failure model for run subscriptions.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RunStreamFailure,
} from "../internal/domain.js";

/**
 * Public repository symbol kind model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Repository symbol kind model re-export.
 *
 * @example
 * ```ts
 * import { RepoSymbolKind } from "@beep/repo-memory-model"
 *
 * const schema = RepoSymbolKind
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Repository symbol kind model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RepoSymbolKind,
} from "../internal/domain.js";

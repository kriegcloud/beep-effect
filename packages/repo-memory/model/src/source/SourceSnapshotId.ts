/**
 * Public source snapshot identifier model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Stable source snapshot identifier model re-export.
 *
 * @example
 * ```ts
 * import { SourceSnapshotId } from "@beep/repo-memory-model"
 *
 * const schema = SourceSnapshotId
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Stable source snapshot identifier model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  SourceSnapshotId,
} from "../internal/domain.js";

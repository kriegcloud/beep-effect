/**
 * Public repository identifier model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Stable repository identifier model re-export.
 *
 * @example
 * ```ts
 * import { RepoId } from "@beep/repo-memory-model"
 *
 * const schema = RepoId
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Stable repository identifier model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  RepoId,
} from "../internal/domain.js";

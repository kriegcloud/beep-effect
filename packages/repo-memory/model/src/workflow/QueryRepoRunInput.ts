/**
 * Public query repository workflow input model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Query repository workflow input model re-export.
 *
 * @example
 * ```ts
 * import { QueryRepoRunInput } from "@beep/repo-memory-model"
 *
 * const schema = QueryRepoRunInput
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Query repository workflow input model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  QueryRepoRunInput,
} from "../internal/domain.js";

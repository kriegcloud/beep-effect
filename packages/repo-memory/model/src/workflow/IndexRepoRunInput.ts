/**
 * Public index repository workflow input model re-export.
 *
 * @packageDocumentation
 * @since 0.0.0
 */

/**
 * Index repository workflow input model re-export.
 *
 * @example
 * ```ts
 * import { IndexRepoRunInput } from "@beep/repo-memory-model"
 *
 * const schema = IndexRepoRunInput
 * ```
 *
 * @category domain model
 * @since 0.0.0
 */
export {
  /**
   * Index repository workflow input model.
   *
   * @since 0.0.0
   * @category DomainModel
   */
  IndexRepoRunInput,
} from "../internal/domain.js";

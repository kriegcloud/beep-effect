/**
 * installer channels server test layer.
 *
 * @packageDocumentation
 * @category testing
 * @since 0.0.0
 */

import { InstallerChannelsServerLive } from "./Layer.js";

/**
 * Deterministic test layer for the installer-channels slice.
 *
 * @category testing
 * @since 0.0.0
 */
export const InstallerChannelsServerTest = InstallerChannelsServerLive;

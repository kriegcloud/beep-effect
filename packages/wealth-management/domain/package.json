{
  "name": "@beep/wm-domain",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Wealth management domain entities, schemas, and ontology integration for high-net-worth client data modeling",
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "directory": "dist",
    "linkDirectory": false
  },
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json",
    "./entities": "./src/entities/index.ts",
    "./ontology": "./src/ontology/index.ts",
    "./*": "./src/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:kriegcloud/beep-effect.git",
    "directory": "packages/wealth-management/domain"
  },
  "scripts": {
    "build": "bun run build-esm && bun run build-cjs && bun run build-annotate",
    "dev": "tsc -b tsconfig.build.json --watch",
    "build-esm": "tsc -b tsconfig.build.json",
    "build-cjs": "babel build/esm --plugins @babel/transform-export-namespace-from --plugins @babel/transform-modules-commonjs --out-dir build/cjs --source-maps",
    "build-annotate": "babel build/esm --plugins annotate-pure-calls --out-dir build/esm --source-maps",
    "check": "tsc -b tsconfig.json",
    "test": "bun test",
    "coverage": "bun test --coverage",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "lint:circular": "bunx madge -c ."
  },
  "peerDependencies": {
    "@beep/shared-domain": "workspace:^",
    "@beep/schema": "workspace:^",
    "effect": "catalog:",
    "@effect/sql": "catalog:"
  },
  "devDependencies": {
    "@effect/docgen": "catalog:",
    "@beep/shared-domain": "workspace:^",
    "@beep/schema": "workspace:^",
    "@beep/testkit": "workspace:^",
    "effect": "catalog:",
    "@effect/sql": "catalog:",
    "@total-typescript/ts-reset": "catalog:"
  },
  "effect": {
    "generateExports": {
      "include": [
        "**/*.ts"
      ]
    },
    "generateIndex": {
      "include": [
        "**/*.ts"
      ]
    }
  }
}

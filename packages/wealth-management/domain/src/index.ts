/**
 * @beep/wm-domain
 * Wealth management domain entities, schemas, and ontology integration
 * for high-net-worth client data modeling.
 *
 * This module contains:
 * - Entity models (Client, Account, Investment, Trust, Household, Beneficiary, Custodian, LegalEntity)
 * - Ontology integration (namespace, class IRIs, property IRIs)
 * - Value objects and error types
 *
 * @module wm-domain
 * @since 0.1.0
 */
export * as Entities from "./entities";
export * as Ontology from "./ontology";

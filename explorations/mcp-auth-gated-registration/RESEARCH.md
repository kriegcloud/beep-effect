# MCP Auth-Gated Registration & Progressive Disclosure — Research

<!--
Stage 1 synthesis. Two halves: cited external prior art, and the in-repo
substrate to compose against. Raw per-subtopic detail lives in research/*.md
(linked inline); this file is the reconciled, cited summary. Dated 2026-06-29.
-->

## External Landscape

Five build-list themes (CAPTURE netNew #1–#5) layered onto beep's two existing
Effect MCP servers. Raw detail per theme:
[research/conditional-credential-keyed-toolkit-composition.md](research/conditional-credential-keyed-toolkit-composition.md),
[research/structured-api-key-required-helper-and-env-auth-matrix.md](research/structured-api-key-required-helper-and-env-auth-matrix.md),
[research/tier-gating-and-tool-governance-ethical-wall.md](research/tier-gating-and-tool-governance-ethical-wall.md),
[research/progressive-disclosure-field-tiers-and-response-reshaping.md](research/progressive-disclosure-field-tiers-and-response-reshaping.md),
[research/tool-definition-annotation-and-routing-conventions.md](research/tool-definition-annotation-and-routing-conventions.md).

### The Effect v4 AI primitives (the framework we build on)

- The repo runs `effect@4.0.0-beta.91` and both MCP drivers import `{ Tool, Toolkit }` from `effect/unstable/ai` (NOT the legacy `@effect/ai` v3 package), with the server from `effect/unstable/ai/McpServer`. `Toolkit.make(...tools)` is the typed registry; `McpServer.toolkit(tk)` registers it; `McpServer.layerStdio({name,version})` provides stdio transport. The public docs at <https://effect-ts.github.io/effect/ai/ai/Tool.ts.html> document the v3 API — the v4 ground truth is `node_modules/effect/src/unstable/ai/{Tool,Toolkit,McpServer}.ts`. Effect Tool Use docs: <https://effect.website/docs/ai/tool-use/>.
- The four MCP behavior hints are first-class Effect `Context.Reference<boolean>` annotations whose defaults exactly equal the MCP wire defaults — `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:true` — so an unannotated tool is treated as writing/destructive/non-idempotent/open-world. `McpServer` maps them 1:1 onto the wire `annotations` object plus `_meta` from `Tool.Meta`. MCP spec defaults verified three ways: spec <https://modelcontextprotocol.io/specification/2025-06-18/server/tools>, design blog <https://blog.modelcontextprotocol.io/posts/2026-03-16-tool-annotations/>, and the effect source defaults.

### #1 Conditional / credential-keyed Toolkit composition

- The Effect-idiomatic optional-credential idiom is `Config.redacted("X_API_KEY").pipe(Config.option)` (missing → `Option.none`, never fails) and `Config.withDefault`; a required missing `Config` fails with `ConfigError`/`MissingData`. Source: <https://effect.website/docs/configuration/>.
- Conditional registration has two complementary strategies in the MIT prior art: **conditional mounting** — `patents-mcp-server` `registerAllTools` only mounts a tool group when its creds resolved (`if(config.usptoApiKey){...} if(config.epoConsumerKey && config.epoConsumerSecret){...} registerUtilityTools // always`) — leaner surface, absent-key tools invisible; vs **always-register + handler guard** (mcp-uspto), more discoverable, larger surface. A faithful Effect port reads `Config.option` at **layer-build time** to make a tool *disappear* (Shapes A/B), or at **call time** to keep it present with a graceful error (Shape C). `lzinga/us-gov-open-data-mcp` (MIT) adds the schema-first carrier: a per-module `ModuleMeta { auth?: { envVar: string|string[]; signup } }` declaration that drives gating, the helper, and the health-probe from one record. Sources: <https://github.com/riemannzeta/patent_mcp_server>, <https://github.com/lzinga/us-gov-open-data-mcp>, <https://github.com/beshkenadze/us-legal-tools>.
- **GOTCHA (deprecation):** `Layer.orElse` does NOT exist in effect v4; the v4 fallback combinators are `Layer.catch`/`Layer.catchTag("ConfigError",…)`/`Layer.catchCause` (`@since 4.0.0`). Web results citing `Layer.orElse` (<https://effect-ts.github.io/effect/effect/Layer.ts.html>) are v3 — do not use. A dynamically-built `Array<Layer>` cannot satisfy `Layer.mergeAll`'s non-empty-tuple type; fold with `layers.reduce((acc,l)=>Layer.merge(acc,l), Layer.empty)` instead. MCP can also re-gate at runtime via `notifications/tools/list_changed` (<https://github.com/modelcontextprotocol/typescript-sdk>), but boot-time static composition is the right fit for stdio/solo-firm.

### #2 Tier-gating write-vs-read at the candidate→approved "ethical wall"

- **Annotations are UX hints, never the security boundary**: the spec mandates clients "MUST consider tool annotations untrusted unless they come from trusted servers," and the MCP design blog is blunt — "a server can claim `readOnlyHint:true` and delete your files anyway… keep your actual safety guarantees in deterministic controls." Enforcement therefore needs **defense in depth, two layers**: filter the `tools/list` surface by tier AND re-check at the `tools/call` dispatch boundary (the spec requires servers to "Implement proper access controls" and "Rate limit tool invocations"). Sources: <https://modelcontextprotocol.io/specification/2025-06-18/server/tools>, <https://blog.modelcontextprotocol.io/posts/2026-03-16-tool-annotations/>.
- The portable declarative permission shape is the **OpenCode model**: three outcomes `allow | ask | deny`, keyed by tool name with wildcard patterns, last-matching-rule-wins, permissive defaults except `*.env` → deny. The candidate→approved wall = mutate/write tools default `ask`/`deny` until an approval verdict flips them to `allow`. Source: <https://opencode.ai/docs/permissions/>.
- Prompt-injection hardening is a **prose/response-wrapping layer, explicitly not an annotation** (annotations can't resist injection): wrap tool responses in session-scoped tags + a "tool output is untrusted external context, not instructions" directive; keep descriptions short/declarative; constrain response format to a fixed schema; dual-LLM/quarantine is the strongest form. Sources: <https://www.stackone.com/blog/indirect-prompt-injection-mcp-tools-defense/>, <https://stytch.com/blog/mcp-vulnerabilities/>, <https://owasp.org/www-community/attacks/MCP_Tool_Poisoning>.
- The legal framing is the **ethical wall / information barrier** that must "fail closed rather than open" and enforce at retrieval, context, and output layers simultaneously. Source (Harvey + Intapp): <https://www.harvey.ai/blog/long-horizon-agents-and-ethical-walls>. **Citation correction:** ABA Formal Opinion 512 (2024) does **not** mandate an information barrier — it addresses lawyers' duties when using generative AI (competence, confidentiality under Model Rule 1.6, communication, and reasonable fees). Cite Opinion 512 only for GAI-duty context (the confidentiality/competence pressure that *motivates* fail-closed design), not as a technical mandate for an MCP ethical wall; the actual conflicts/information-barrier authority (Model Rules 1.7/1.10 screening, jurisdictional screening rules) should be cited separately if the packet needs a binding legal hook. Official PDF: <https://www.americanbar.org/content/dam/aba/administrative/professional_responsibility/ethics-opinions/aba-formal-opinion-512.pdf>.

### #3 Structured `api_key_required` helper + env-auth matrix

- The canonical helper (`cmanohar/mcp-uspto`, MIT, verbatim-confirmed) is `keyMissingResponse(envVar, registrationUrl, toolName)` returning a **normal tool result** whose `content[].text` is `JSON.stringify({ error:"api_key_required", tool, message:"…Set the ${envVar} environment variable.", registration })` — returned as content, NOT thrown, "so the LLM gets helpful guidance." Sources: <https://raw.githubusercontent.com/cmanohar/mcp-uspto/master/src/lib/config.ts>.
- This is **spec-correct against the installed protocol target** — but pin the version. The bundled `effect@4.0.0-beta.91` `McpServer` advertises `LATEST_PROTOCOL_VERSION = "2025-06-18"` and negotiates only `2025-06-18 | 2025-03-26 | 2024-11-05 | 2024-10-07`; it does **NOT** speak `2025-11-25` (`node_modules/effect/src/unstable/ai/McpServer.ts:336-341,1344-1346`). Both the 2025-06-18 and 2025-11-25 specs define the same two error channels — Protocol Errors (JSON-RPC `error`) and Tool Execution Errors (normal `result` + `isError:true`) — and say clients "SHOULD provide tool execution errors to language models to enable self-correction," whereas protocol errors "are less likely to result in successful recovery." A missing key is a recoverable tool-execution outcome. **Design the channel to 2025-06-18 semantics; any reliance on 2025-11-25-specific behavior is spec drift and requires an effect dependency-upgrade + reverification task first.** Ecosystem gotcha: several clients mishandle `structuredContent` (validation runs before `isError`; adapters have dropped it), so mirror the structured payload into a `content[].text` JSON string. Sources: <https://modelcontextprotocol.io/specification/2025-06-18/server/tools>, <https://modelcontextprotocol.io/specification/2025-11-25/server/tools>, <https://github.com/modelcontextprotocol/typescript-sdk/issues/654>.
- The env-auth matrix (`beshkenadze/us-legal-tools`, MIT): CourtListener→`COURTLISTENER_API_TOKEN`, GovInfo→`GOV_INFO_API_KEY`, DOL→`DOL_API_KEY`; eCFR + Federal Register → no auth. **Adversarial nuance — "auth required" collapses three gate strengths**, so the Effect matrix should encode `gate: none|soft|hard`, not a boolean: no-auth/public (eCFR, Federal Register), hard-gate (GovInfo 401 `API_KEY_MISSING`; DOL APIv4), soft-gate (CourtListener open-by-default + throttled). Sources: <https://github.com/beshkenadze/us-legal-tools>, <https://github.com/usgpo/api>, <https://wiki.free.law/c/courtlistener/help/api/rest/v4/overview>.
- The `check-api-status` probe (`patents-mcp-server` `getAvailableSources(cfg): {name,configured,healthy}[]`, MIT) has a known bug to fix on port: `configured` is presence-only and `healthy` is hardcoded `false` (never pings upstream). A truthful probe sets `healthy` from a real cheap request, surfacing 401/403 distinctly from 5xx. **Underspecified for shape (advisory):** before graduating, define a per-source probe matrix — probe endpoint, auth transport, timeout budget, cache/TTL, expected unauthenticated response (so 401/403 ≠ outage), soft-gate behavior for CourtListener, offline behavior, and a redacted result schema that never leaks credential state — since this tool is flaky, rate-limited, and credential-sensitive by nature.

### #4 Progressive-disclosure field tiers + server-side response reshaping

- **The hard constraint making reshaping mandatory:** Claude Code rejects any single MCP tool output above **25,000 tokens** (`MAX_MCP_OUTPUT_TOKENS`), erroring "MCP tool response exceeds maximum allowed tokens (25000)". A raw USPTO PFW search with `documentBag` trivially blows past it — reshaping is a correctness requirement, not a nicety. Tool *definitions* are also an upfront tax: Anthropic measured **150,000 tokens** on tool defs + intermediate results, reduced to ~2,000 (98.7%) via code execution. Sources: <https://help.xpoz.ai/en/articles/12681842-claude-code-mcp-tool-exceeds-maximum-allowed-tokens-25000>, <https://github.com/anthropics/claude-code/issues/9152>, <https://www.anthropic.com/engineering/code-execution-with-mcp>.
- **Named field tiers** (canonical: `john-walkoe/uspto_pfw_mcp` `field_configs.yaml`, MIT): `applications_minimal` (~14 dotted-path fields, claims 95–99% reduction), `applications_balanced` (~18 fields, 85–90%); `documentBag` is commented out with a verbatim WARNING "Can cause 100x token increase — use pfw_get_application_documents instead." The three-rung ladder (ultra-minimal 2–3 fields→99% / minimal 15→95% / balanced 18→85–90%) is documented in CUSTOMIZATION.md. USPTO ODP supports server-side `fields` projection at `POST /applications/search` (optional body param). Sources: <https://github.com/john-walkoe/uspto_pfw_mcp>, <https://github.com/john-walkoe/uspto_pfw_mcp/blob/master/CUSTOMIZATION.md>, <https://data.uspto.gov/apis/patent-file-wrapper/search>.
- **Reshaping levers**: `format=csv` (~40–70% on tabular), `format=outline|tree` (~91%), `fields=` whitelist (~80–90%) from `screenpipe` (license NOASSERTION → reimplement conventions, do not copy); columnar envelope (`toColumnar` + `stripNulls` + `detectTrend` linear-regression trend, row caps) from `lzinga/us-gov-open-data-mcp` (MIT). Independent guidance puts CSV lower (40–60%), so treat 40–90% as the defensible workload-dependent band. The **metadata→snippet→read tool ladder** (cheap counts → keyword snippet → full text by id, with per-turn caps) is corroborated by screenpipe's workflow + Anthropic's progressive-disclosure framing (the `mike` CourtListener ladder is unknown-license → reimplement). Large byte payloads (PDFs) go behind **UUID+TTL fetchable handles / MCP Resources** (`resources/read`, strict UUID regex gating fs access — no path interpolation), never inline. Sources: <https://raw.githubusercontent.com/screenpipe/screenpipe/main/.claude/skills/screenpipe-api/SKILL.md>, <https://raw.githubusercontent.com/lzinga/us-gov-open-data-mcp/main/src/shared/response.ts>, <https://modelcontextprotocol.io/specification/2025-06-18/server/resources>.
- **Out of scope for v1**: WASM code-mode (QuickJS/V8-isolate sandbox; Cloudflare reports 1.17M→~1k tokens / 99.9%) is a server-wide capability with heavyweight infra; field tiers + columnar reshaping + Resources already capture 95–99% of the win for stdio servers. The method-enum mega-tool (one `uspto_patents(method,…)` vs ~25 discrete tools) is the **wrong** axis for Effect — schema validation is free, discrete typed tools are cheap. Sources: <https://blog.cloudflare.com/code-mode-mcp/>, <https://github.com/jmpijll/fortimanager-code-mode-mcp>.

### #5 readOnly/idempotent hint conventions + USE-WHEN routing prose

- There is **no structured "use-when" field** — routing prose lives in the free-text `description` (or the params-schema description); the host injects it into the model prompt. Anthropic confirms description quality is high-leverage, not cosmetic (precise tool-description refinement drove Claude Sonnet to SOTA on SWE-bench Verified): state what it does and when to use it, third-person, with concrete input examples. This backs porting the screenpipe USE-WHEN / DO-NOT-USE + "Start with limit=5" prose convention. Per-field descriptions port Zod `.describe()`/`.default()` (us-legal-tools, MIT) into effect Schema annotations. Sources: <https://www.anthropic.com/engineering/writing-tools-for-agents>, <https://platform.claude.com/docs/en/agents-and-tools/tool-use/define-tools>.

## In-Repo Capability Inventory

beep-effect already owns the entire MCP substrate; this packet is **patterns layered onto it**, not new servers. All paths verified via `rg`/`ls` on 2026-06-29.

### MCP server scaffold + composition seam (compose, do not re-scaffold)

- `@beep/nlp-mcp` — `packages/drivers/nlp-mcp/src/Server.ts:104-107` already **mounts two toolkits into one server** via `Layer.mergeAll(McpServer.toolkit(NlpToolkit).pipe(Layer.provide(WinkNlpToolkitLive)), McpServer.toolkit(StreamingToolkit).pipe(Layer.provide(StreamingToolkitHandlersLive))).pipe(Layer.provide(McpServer.layerStdio({...})), Layer.orDie)`. **This `Layer.mergeAll(...)` argument list IS the conditional-registration seam** — credential-keyed composition (#1) slots in here with zero changes to `Tool.make` defs, handler layers, or the `toolkit`/`layerStdio` scaffold. (`Server.ts`, `StreamingTools.ts`, `StreamingHandlers.ts`, `bin.ts`.)
- `@beep/m365-mcp` — `packages/drivers/m365-mcp/src/{Server,M365Tools,M365Handlers}.ts` — single-toolkit read-only v1, same pattern. It is the **only** package that annotates all four hints: `M365Tools.ts:100-103` chains `.annotate(Tool.Readonly,true).annotate(Tool.Destructive,false).annotate(Tool.Idempotent,true).annotate(Tool.OpenWorld,true)` (verified, repeated per tool). The NLP toolkit tools (`packages/foundation/capability/nlp/src/Tools/*`) and `StreamingTools.ts` do NOT annotate hints — the asymmetry (e.g. `CreateCorpus`/`DeleteCorpus` are not read-only) is the concrete #5 convergence target.
- `failureMode: "return"` + a structured `failure` schema is already standard in both servers (`M365Tools.ts`, `StreamingTools.ts`) — the idiomatic in-band channel for the `api_key_required` envelope (#3) and for a tier-gate refusal verdict (#2), replacing the hand-rolled `keyMissingResponse`. **BLOCKING wire-encoding gotcha (verified):** `McpServer.registerToolkit` constructs `CallToolResult({ isError: false, structuredContent, content })` for *any* successfully-returned encoded result and sets `isError: true` only when the Effect *cause* fails (`node_modules/effect/src/unstable/ai/McpServer.ts:708-738`, see `Effect.matchCause` onSuccess/onFailure). A typed `failureMode:"return"` failure (e.g. `ApiKeyRequired`) is a *success* value from the toolkit's perspective, so it ships as `isError:false` — wire-indistinguishable from a real result. Either (a) deliberately adopt the mcp-uspto success-with-error-JSON contract (mirror the typed failure into `content[].text` and accept `isError:false`, and test client self-correction behavior), or (b) wrap `tools/call` registration so returned typed failures map to `isError:true`. Do not assume `failureMode:"return"` yields `isError:true`.

### Config-gated, key-optional drivers (the credential-resolution primitive)

- The optional-credential idiom is real but **narrower than "every driver can disappear"** — split it into four classes (`rg` verified 2026-06-29), because only the first class lets a tool vanish on a missing credential:
  - **Optional *secret* credential** (`Config.redacted(...).pipe(Config.option)`, missing → `Option.none`): `@beep/uspto` (`USPTO_API_KEY`), `@beep/xai` (`XAI_API_KEY`, `XAI_MANAGEMENT_API_KEY`), `@beep/phoenix` (`PHOENIX_API_KEY`), `@beep/runpod` (`RUNPOD_API_KEY`), `@beep/hubspot` (`HUBSPOT_SERVICE_KEY`), `@beep/sanity` (`SANITY_API_TOKEN`). This is the actual primitive #1's "tool disappears on absent key" depends on.
  - **Required *secret* credential** (`Config.redacted(...)`, missing → `ConfigError`): `@beep/anthropic` (`ANTHROPIC_API_KEY`), `@beep/venice-ai` (`AI_VENICE_API_KEY`), `@beep/firecrawl` (`FIRECRAWL_API_KEY`), `@beep/box` (`CLOUD_BOX_TOKEN`) — these *fail the layer* on a missing key; they cannot self-gate to "absent."
  - **Optional *non-secret* config** (`Config.string|number|int(...).pipe(Config.option)`): `@beep/m365` (`M365_AUTHORITY`, `M365_GRAPH_BASE_URL`, `M365_MAX_RETRIES`, `M365_REDIRECT_URI`, `M365_SCOPES`, `M365_TOKEN_CACHE_PATH`), `@beep/hubspot` (`HUBSPOT_ACCOUNT_ID`, CRM/Forms URLs), `@beep/sanity` (project/dataset/version/host), `@beep/firecrawl` (backoff/retries/timeout). These are tuning knobs, **not** credentials — M365's `Config.option` usages are all non-secret config, not key-gating.
  This is the single primitive both registration strategies (#1) build on, but the gating logic must key off the *optional-secret* class only.
- `@beep/uspto` — `packages/drivers/uspto/src/Uspto.service.ts` is already built **key-optional**: resolves `apiKey: Option<Redacted>` and only sets `X-API-KEY` when present, targeting ODP `api.uspto.gov` (`Uspto.config.ts`, NOT PatentsView). This is the in-repo precedent for "driver self-gates on credential availability" (Shape C). `Uspto.models.ts` `UsptoApplicationMetadata` already projects an implicit ~11-field "minimal" tier, and `documentBag` already lives behind a separate `getDocuments` (`Uspto.service.ts:65,322`) — the #4 field-tier pattern is a *generalization of what the driver already does*, not a new concept.
- LLM-driver Config usage (boundary evidence that the LLM-fallback cluster resolves creds independently): `@beep/anthropic` `Anthropic.service.ts:36`, `@beep/xai` `XAi.service.ts:1020-1021`, `@beep/venice-ai` `VeniceAI.service.ts:1877`.

### Governance / approval / audit substrate (#2 plumbing already exists)

- `@beep/epistemic-use-cases` **`ClaimGate`** — `packages/epistemic/use-cases/src/ClaimGate/ClaimGate.service.ts` — `evaluate(claim, evidence)` runs a bounded SHACL check returning an `admitted | rejected` verdict; **rejection is a value, never an error; the engine is total**. The write-tool wall (#2) should mirror this shape (refusal as structured content, fail-closed). Ports declared in `ClaimGate.ports.ts`.
- `@beep/epistemic-domain` audit sink — `packages/epistemic/domain/src/entities/{Activity,UsageRecord}/` — `Activity` (`{fixtureKey, snapshot: jsonb}`) + `UsageRecord` (`{activityId, actor: Principal, provider, model, metadata, credentialReference, latency/token/cost}`). **CORRECTION (verified — only half this persistence exists):** `@beep/epistemic-tables` exports only `Entities.UsageRecord` and a `DbSchema` containing only `usageRecord` (`packages/epistemic/tables/src/{index.ts,Schema.ts}`), and the sole epistemic migration creates `epistemic_usage_record`. There is **no `Activity` table or converter** in `@beep/epistemic-tables` (`rg "Activity" packages/epistemic/tables/src` → zero hits) — the `Activity` *domain model* exists in `@beep/epistemic-domain` but is not persisted here, while `UsageRecord` carries an `activityId` column pointing at an activity that is not itself stored. So a gated `tools/call` cannot yet be "one persisted `Activity` + one `UsageRecord`." **FIX:** either add/reuse persisted `Activity` support (table + converter + `DbSchema` entry) as part of this work, or store the tool-invocation snapshot in `UsageRecord.metadata` and drop the one-persisted-`Activity`-per-call promise.

### Schema / identity / decode primitives (#5 + helper plumbing)

- `@beep/schema` identity composer — `packages/foundation/modeling/identity/src/Id.ts` — the `n`/`nSchema`/`nKey`/`nHttp` family backing `$I.annote(...)`/`annotateKey({description})`, already used for per-field descriptions in both servers. The home for porting Zod `.describe()` routing prose into effect Schema.
- Decode-and-map pattern — `packages/foundation/modeling/schema/src/Jsonl.ts:64-81` — `S.decodeUnknownEffect(...) + Effect.mapError(...)`, the repo precedent for a `ToolValidationError` (replaces the v3 `ParseResult.TreeFormatter.formatErrorSync`, which does NOT exist in v4 — use `effect/SchemaIssue` `makeFormatterStandardSchemaV1()`).

### Gov-legal data drivers (the consumers of this gating layer)

- `@beep/courtlistener`, `@beep/ecfr`, `@beep/dol`, `@beep/federal-register` — bare skeletons (1 src file each, verified) → built by `gov-legal-data-driver-codegen`. `@beep/govinfo` substantial (~27 files per tree-snapshot, extend in place). `@beep/epo` — **does not exist** (`packages/drivers/epo` absent). These are the keyed sources whose registration this packet gates.

### Genuine gaps (no real src implements these today)

- `api_key_required` structured helper — **NOT FOUND**. `rg "api_key_required|keyMissing"` across `packages/` → zero hits.
- `requiresAuth` / tier-gate / premium-flag tool gating — **NOT FOUND**. `rg "requiresAuth"` → zero hits.
- Typed `ToolRegistry` / `ToolMetadata` carrying auth/category/tier — **NOT FOUND**. `rg "ToolRegistry|ToolMetadata"` → zero hits.
- `USE WHEN` / `DO NOT USE` routing-prose convention in descriptions — **NOT FOUND**. `rg "USE WHEN|DO NOT USE"` → zero hits.
- Named progressive field tiers (`minimal`/`balanced`/`complete`) as explicit Schema variants — **NOT FOUND** as a named abstraction (only the implicit 11-field projection in `UsptoApplicationMetadata`).
- A `SourceAuth` / availability-matrix registry (`{name, envVar, gate, signupUrl}`) and a `check-api-status` health-probe tool — **NOT FOUND**.

## Constraints

### Deprecations & sunsets (dated)

- **PatentsView shut down 2026-03-20**; `api.patentsview.org` 301-redirects to `data.uspto.gov/odp` (HTML, not JSON). The Office Action and Enriched Citation APIs were decommissioned in early 2026. Target USPTO **ODP `api.uspto.gov` only** from the start (`@beep/uspto` already does); encode the sunset as a tagged error. Source: <https://github.com/riemannzeta/patent_mcp_server>.
- **`Layer.orElse` removed in effect v4** (`4.0.0-beta.91`). Use `Layer.catch`/`Layer.catchTag("ConfigError",…)`/`Layer.catchCause`. v3 docs (<https://effect-ts.github.io/effect/effect/Layer.ts.html>) and the v3 `@effect/ai` Tool docs are stale references — read `node_modules/effect/src/unstable/ai/*` as ground truth.
- **`ParseResult.TreeFormatter.formatErrorSync` removed in v4** — the research-squad dispatch pattern uses it; v4 equivalent is `effect/SchemaIssue` `makeFormatterStandardSchemaV1()` / `defaultFormatter`.
- **`effect/unstable/ai` is beta** — the annotation API (`annotateMerge`, `Context.Reference` hints) and MCP mapping may shift before stable; pin and re-verify at implementation time.
- **DOL API version split** — legacy `api.dol.gov/V1` uses `?KEY=` query param (deprecated); the modern DOL Open Data Portal + APIv4 at `dataportal.dol.gov` (launched ~2026-02-18) uses header-based auth (exact header name UNVERIFIED — confirm before wiring `@beep/dol`). Source: <https://dataportal.dol.gov/>.

### Licensing gravity (port-source provenance)

- **Verified MIT — clean to port (still reimplement to Effect idiom, do not literal-copy):** `cmanohar/mcp-uspto`, `beshkenadze/us-legal-tools`, `riemannzeta/patent_mcp_server`, `john-walkoe/uspto_pfw_mcp`, `lzinga/us-gov-open-data-mcp`. The patterns sourced *directly* from these (the `keyMissingResponse` helper shape, the env-auth matrix, the field-tier configs, the columnar/`stripNulls`/trend envelope) are port-unblocked.
- **CAPTURE-derived / unresolved-license — REIMPLEMENT-ONLY until repo identity + LICENSE confirmed:** `uspto-patents-mcp` and `patents-mcp` were *not* independently license-resolved this pass, and the TypeScript `patents-mcp-server` (distinct from the MIT-verified Python `riemannzeta/patent_mcp_server`) is unresolved (see next bullet). The bodies attributed to these — `registerAllTools` conditional mounting, `getAvailableSources`/`check-api-status`, and the UUID+TTL fetchable-handle store — are therefore **NOT** covered by "port path unblocked"; treat them as reimplement-from-spec only until the exact repository and LICENSE are confirmed. Do not literal-copy those bodies.
- **NOASSERTION / "Other" → REIMPLEMENT, DO NOT COPY:** `screenpipe` (resolves `spdx_id: NOASSERTION` via GitHub API — not a clean OSS grant). Re-author the `format`/`fields` reshaping conventions; the percentages and field names are facts, the code is not licensed for copy.
- **Unknown / AGPL → REIMPLEMENT, DO NOT COPY:** `mike` (CourtListener metadata→find→read ladder + governance gate) is recorded **AGPL-3.0-only / unknown-license** in the gold catalog; `doc-haus` and `harvest-mcp` flagged unknown. The patterns (ladder, permission matrix, untrusted-context suffix, audit pairing) are corroborated by the public MCP spec/blog + OpenCode — reimplement from those, treat the CAPTURE snippets as provenance only.
- **TS `patents-mcp-server` LICENSE not independently re-resolved** this pass (distinct from the MIT-verified Python `riemannzeta/patent_mcp_server`); confirm the repo identity + LICENSE before porting the `registerAllTools`/`getAvailableSources`/UUID+TTL-store bodies.

### Auth / secret / offline boundaries

- **`api_key_required` is content, not a thrown error** (spec-correct against 2025-06-18, recoverable). Mirror the structured payload into `content[].text` JSON — do not rely on `structuredContent` alone (clients drop it). **RESOLVED (was UNVERIFIED):** the Effect `McpServer` does **NOT** set `isError:true` on a `failureMode:"return"` typed failure — every successfully-returned encoded result (success *or* typed failure) ships as `CallToolResult({ isError: false, … })`; `isError:true` is reserved for a failed Effect cause (`McpServer.ts:708-738`). So the channel is the success-with-error-JSON contract by default; if you need `isError:true` semantics, add a `tools/call` registration wrapper that maps typed failures onto it.
- **Gate strength is a 3-value enum, not a boolean** (`none | soft | hard`). Encoding CourtListener as a hard boolean mis-gates it — model it as optional-token + graceful degradation (Shape C), robust to the 2026 tightening (anonymous 401 / membership-gating reported, but sources conflict; see below).
- **Tool annotations are untrusted hints, never the security boundary** — enforce tier-gating at BOTH `tools/list` (surface filter, UX/token-economy) AND `tools/call` (real dispatch gate). There are **three distinct metadata channels**, don't conflate them: (1) *internal enforcement metadata* — custom `Context.Reference` annotations stay in `tool.annotations` and are **not** emitted to the wire; they must be read by a beep-side dispatch wrapper. (2) *host-visible advisory metadata* — `Tool.Meta` (`node_modules/effect/src/unstable/ai/Tool.ts:1759`) **IS** emitted by `McpServer` to the wire as `_meta` (`McpServer.ts:691,706`), so anything placed there is host-visible and still untrusted. (3) `needsApproval` (`boolean | NeedsApprovalFunction`) on `Tool.make` (`Tool.ts:274,1267,1409`) — an Effect-level approval API, but **no `McpServer` enforcement of it was found**, so it is relevant prior art / an opt-in to evaluate or deliberately reject, not a ready-made gate.
- **`tools/list` *does* have a built-in filter hook (corrects the earlier "no public hook" gap):** `McpSchema.EnabledWhen` (`McpSchema.ts:2525`) is a `Context.Service` predicate over the *initialized client* payload, read by `McpServer.filterByClient` so `tools/list` emits only items whose predicate passes (`McpServer.ts:1455,1490-1512`). Limits: it keys off client-*initialization* params (not per-call tier), and `tools/call` dispatches by name through the tool map **without re-checking `EnabledWhen`** (`McpServer.ts:708-738`). So `EnabledWhen` is the list-surface filter only — pair it with the separate `tools/call` authorization wrapper for the real boundary.
- **Fail-closed (ethical wall):** write/mutate tools default `ask`/`deny` at the candidate stage, unlocking only on an explicit `ClaimGate`-shaped approval verdict. **Span-hygiene CORRECTION (verified — NOT already enforced on the generic path):** `node_modules/effect/src/unstable/ai/Toolkit.ts:263-266` annotates every tool span with the full `parameters: params`, and on decode failure also records `toolParams: params` (`Toolkit.ts:289-292`). `@beep/nlp` tools (`Analyze`, `Tokenize`, `TransformText`, `ExtractEntities`, `WordCount`) accept raw `text`, so raw user content lands in spans despite the M365/streaming handlers' count/path/size discipline. **RISK:** add a sanitized span wrapper (or disable full-parameter span annotation for MCP calls) and a proof test *before* claiming audit/span hygiene — the count/size/path discipline is in the handlers, not in the upstream toolkit span.
- **Secret discipline:** credentials resolve via `Config.redacted` (`Redacted<string>`, logs as `<redacted>`); DOL docs explicitly ban committing keys to public repos — beep-effect is a PUBLIC repo, so no keys/strategy in-tree.

### Locked decisions / routing cautions

- **Target MCP surface is UNRESOLVED — decide before decompose (was overstated as "do NOT scaffold a third").** Filesystem verification (2026-06-29): only `packages/drivers/nlp-mcp` and `packages/drivers/m365-mcp` import `McpServer`/`Tool.make`/`Toolkit.make`; `@beep/uspto`, `@beep/courtlistener`, `@beep/govinfo`, `@beep/dol`, `@beep/ecfr`, and `@beep/federal-register` have **no MCP surface at all** (`rg "McpServer|Tool.make|Toolkit.make" packages/drivers/{uspto,courtlistener,govinfo,dol,ecfr,federal-register}` → zero hits). So the gov-legal consumers named below (#83) cannot be "layered onto the two existing servers" — neither existing server is a gov-legal host, and `research/conditional-credential-keyed-toolkit-composition.md` itself assumes a **new gov-legal MCP `makeServerLayer`**, contradicting "do NOT scaffold a third." **Pick one before shaping:** (a) extend an existing server (NLP/M365) with gov-legal toolkits, (b) create a dedicated gov-legal/USPTO MCP package that reuses the nlp-mcp `Layer.mergeAll(...)` seam verbatim, or (c) scope this packet to *reusable patterns* (auth-gating, progressive disclosure) consumed by later driver-specific MCP work. The patterns themselves (auth-gating, progressive disclosure) still layer onto the existing `Layer.mergeAll(...)` seam regardless of which host is chosen.
- **Absorbs null nugget `patents-mcp-server#12`** (multi-provider config loader → `getAvailableSources`/`check-api-status`).
- **Boundary WITH — not absorption OF — the multi-provider LLM dispatch/fallback cluster.** They share one primitive (`Config.redacted`) but sit on different axes: this packet = build-time *source* capability gating (which tools exist); LLM-fallback = runtime request-time *provider* selection with key-precedence + failover (which model answers). If a tool needs an LLM it depends on a provider-selection PORT (`Context.Tag`), never embedding precedence/fallback. Cross-link to the proposed `multi-provider-llm-dispatch-fallback` packet (gapStatus partial/P2); do not pull it in.
- **NLP tool-surface expansion + streaming** is owned by `goals/nlp-adjunct-port` (DONE) — not part of this cluster. USPTO driver depth (ODP-only, no codegen restart) is `uspto-patent-driver-depth`; gov-legal driver builds are `gov-legal-data-driver-codegen`. This packet coordinates those consumers, it does not build the drivers.
- **Code-mode/WASM sandbox: out of scope for v1** stdio servers — revisit only if multi-call composition dominates a turn.

### Open / unverified (inherit as risks)

- **CourtListener auth is genuinely contradictory across 2026 sources** (wiki "open by default" vs blog/changelog "anonymous 401 + membership-gated since 2026-05-07"). Recommendation (optional-token + graceful degradation) is deliberately robust to either; resolve before locking the gating tier. Sources: <https://wiki.free.law/c/courtlistener/help/api/rest/v4/overview>, <https://free.law/2026/05/07/api-included-in-memberships/>.
- **USPTO ODP `fields` semantics (load-bearing):** whether `fields` projects the response body (push tier to the wire) or only selects sortable fields is unsettled across secondary sources; the authoritative `ODP-API-Query-Spec.pdf` did not render. Verify before deciding API-side vs client-side projection.
- **`McpServer` list-filtering hook** — RESOLVED (was "no public hook"): `McpSchema.EnabledWhen` is the built-in list filter (`Context.Service` predicate over the initialized-client payload, read by `McpServer.filterByClient`, `McpServer.ts:1490-1512`). It filters `tools/list` only — it does **not** re-check at `tools/call`, and keys off client-initialization params, not per-call tier. Tier-gating still needs both a build-time tier-specific `Toolkit` (or `EnabledWhen` predicate) for the surface AND a `tools/call` dispatch wrapper for the boundary; both axes are now sourced (see Auth/secret/offline boundaries).
- **Custom `Context.Reference` annotation wire survival** — RESOLVED: arbitrary custom `Context.Reference` annotations stay internal-only (not emitted), but `Tool.Meta` *is* emitted to the wire as `_meta` (`McpServer.ts:691,706`), and `Tool.make` exposes a `needsApproval` option (unenforced by `McpServer`). Keep the real gate in the dispatch Layer; use `Tool.Meta` only for host-visible advisory metadata.

---

_Codex gate-1 folded 2026-06-29: 6 blocking + 5 advisory addressed._

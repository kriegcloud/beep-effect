# Codex research-gate critique — effect-orchestration-patterns (2026-06-29)

## Blocking

1. **M365 retry prior art omitted** — Claim: "`@beep/anthropic` ... the ONLY driver with a real inline retry policy" (`explorations/effect-orchestration-patterns/RESEARCH.md:60`). Wrong: `@beep/m365` parses `retry-after` and recursively retries 429/503 within a configured budget (`packages/drivers/m365/src/M365.service.ts:769`, `packages/drivers/m365/src/M365.service.ts:800`, `packages/drivers/m365/src/M365.config.ts:68`, `packages/drivers/m365/test/M365.service.test.ts:520`). Correct state: Anthropic is the only `Schedule`/`ExecutionPlan` retry, but M365 is real driver retry prior art; add it to the extraction inventory and migration plan.

2. **Existing degraded fan-out marked missing** — Claim: "Current fan-outs set `concurrency` but short-circuit on first failure: `.../GraphOperations/Executor.ts`" and "per-item tagged-status degraded-batch helper ... NOT FOUND" (`explorations/effect-orchestration-patterns/RESEARCH.md:77`, `explorations/effect-orchestration-patterns/RESEARCH.md:78`). Wrong: `@beep/nlp` already uses `Effect.result` per leaf, converts failures into an `errors` array, runs bounded `Effect.forEach`, and folds results (`packages/foundation/capability/nlp/src/Graph/GraphOperations/Executor.ts:236`, `packages/foundation/capability/nlp/src/Graph/GraphOperations/Executor.ts:237`, `packages/foundation/capability/nlp/src/Graph/GraphOperations/Executor.ts:354`, `packages/foundation/capability/nlp/src/Graph/GraphOperations/Executor.ts:306`). Correct state: a generic tagged helper may be net-new, but the repo already has production degraded fan-out prior art to reuse.

## Advisory

1. **Seed typecheck claim is too broad** — Claim: "Every CAPTURE seed snippet ... will not typecheck as-written" (`explorations/effect-orchestration-patterns/RESEARCH.md:19`). Concern: the fan-out seed uses `Effect.forEach(..., { concurrency })`, which matches v4's options (`explorations/effect-orchestration-patterns/CAPTURE.md:43`, `.repos/effect-v4/packages/effect/src/Effect.ts:776`). Suggested improvement: change "every" to "several TypeScript seed snippets" and name the actual broken ones (`Schedule.compose`, `Layer.unwrapEffect`, `Layer.die`, old `zipRight`/`zipLeft` aliases).

2. **Idempotency-Key status is stale** — Claim: "`Idempotency-Key` draft ... is a Working-Group Internet-Draft, NOT yet an RFC (as of 2026-06)" (`explorations/effect-orchestration-patterns/RESEARCH.md:34`). Concern: the current datatracker page marks `draft-ietf-httpapi-idempotency-key-header-07` as "Expired Internet-Draft" with IESG state "Expired" (`https://datatracker.ietf.org/doc/draft-ietf-httpapi-idempotency-key-header/`, lines 112-127). Suggested improvement: cite it as an expired/archive I-D, not an active WG draft; keep the safe-by-default retry guidance.

3. **False package-like citation: `@beep/epistemic`** — Claim: consuming surfaces include "`@beep/epistemic` ingestion" (`explorations/effect-orchestration-patterns/research/failure-vs-defect-error-helpers.md:3`, `explorations/effect-orchestration-patterns/research/failure-vs-defect-error-helpers.md:52`). Concern: no `@beep/epistemic` package exists; the real packages are `@beep/epistemic-domain`, `@beep/epistemic-use-cases`, `@beep/epistemic-server`, and `@beep/epistemic-tables` (`packages/epistemic/domain/package.json:2`, `packages/epistemic/use-cases/package.json:2`, `packages/epistemic/server/package.json:2`, `packages/epistemic/tables/package.json:2`). Suggested improvement: replace the shorthand with exact package aliases, probably `@beep/epistemic-use-cases/ClaimProjection` and `@beep/epistemic-use-cases/ClaimGate`.

4. **`port the algorithm` license wording is unsafe** — Claim: "recommendation is to port the ~80–150-line state machine ... not adopt a dep" and "reference-only ... no license attaches" (`explorations/effect-orchestration-patterns/RESEARCH.md:39`, `explorations/effect-orchestration-patterns/RESEARCH.md:103`). Concern: translating Cockatiel/opossum source closely would still be derivative enough to require license/notice handling even if no dependency is adopted. Suggested improvement: require clean-room implementation from behavioral specs/tests, or explicitly preserve notices if any source is copied or translated.

5. **`AiError.retryAfter` no longer fully unverified** — Claim: "UNVERIFIED: whether `@effect/ai` ... `AiError` exposes the raw provider `retry-after`/reset header (or only `isRetryable`)" (`explorations/effect-orchestration-patterns/research/schedule-retry-policy-library-design.md:88`). Concern: beta.91 `RateLimitError` has `retryAfter`, and top-level `AiError` delegates it (`.repos/effect-v4/packages/effect/src/unstable/ai/AiError.ts:372`, `.repos/effect-v4/packages/effect/src/unstable/ai/AiError.ts:1486`). Suggested improvement: split the open item: typed `retryAfter` is verified; raw reset-header passthrough remains provider-specific and still needs inspection.

## Confirmed sound

- Effect version grounding checked out: root `effect` is `4.0.0-beta.91`, package workspaces consume `catalog:`, and the installed/vendored packages match (`package.json:157`, `node_modules/effect/package.json:4`, `.repos/effect-v4/packages/effect/package.json:4`).
- v4 API deltas checked out: `Effect.forEach` only exposes `concurrency`/`discard`, `Effect.all` has `mode: "result"`, `Layer.unwrap` exists, and `Layer.unwrapEffect`/`Layer.die`/`Layer.fail` have no matches in vendored/package type surfaces (`.repos/effect-v4/packages/effect/src/Effect.ts:776`, `.repos/effect-v4/packages/effect/src/Effect.ts:373`, `.repos/effect-v4/packages/effect/src/Layer.ts:1126`).
- `Layer.unwrap` prior art checked out: active repo call sites include `SqlTest` lazy selection and M365 typed config errors (`packages/tooling/test-kit/test-utils/src/SqlTest.ts:1340`, `packages/drivers/m365/src/M365.service.ts:1176`).
- `@beep/openai-compat` as a Schedule/ExecutionPlan retry gap checked out: its "retry" token is the unsupported SSE directive mapper, not a retry policy (`packages/drivers/openai-compat/src/OpenAiCompatClient.service.ts:247`, `packages/drivers/openai-compat/src/OpenAiCompatClient.service.ts:300`).
- Effect v4 resilience primitives checked out: `RateLimiter`, `HttpClient.withRateLimiter`, `Semaphore.withPermitsIfAvailable`, `PartitionedSemaphore`, `Cache`, and `Schedule.both`/`jittered`/`modifyDelay` are present at the cited vendored surfaces (`node_modules/effect/dist/unstable/persistence/RateLimiter.d.ts:41`, `node_modules/effect/dist/unstable/http/HttpClient.d.ts:743`, `node_modules/effect/dist/Semaphore.d.ts:107`, `node_modules/effect/dist/PartitionedSemaphore.d.ts:1`, `node_modules/effect/dist/Cache.d.ts:83`, `node_modules/effect/dist/Schedule.d.ts:1012`).
- `@beep/schema` as the AI-free error-helper host checked out: it exports the named tagged-error helpers and depends on `effect` plus first-party packages, not `effect/unstable/ai` (`packages/foundation/modeling/schema/src/index.ts:49`, `packages/foundation/modeling/schema/src/index.ts:331`, `packages/foundation/modeling/schema/src/index.ts:336`, `packages/foundation/modeling/schema/src/index.ts:346`, `packages/foundation/modeling/schema/package.json:26`).

# Codex research-gate critique — court-vocabulary-resolver (2026-06-29)

## Blocking

1. **Claim (`RESEARCH.md:182-189`; `research/court-jurisdiction-reporter-vocabulary-schema.md:49`): "`@beep/schema` — `LiteralKit.make(literals, mappings)`" and "Use the repo's existing `LiteralKit.make(literals, mappings)`."** → This API claim is false in the current checkout. `packages/foundation/modeling/schema/src/LiteralKit/LiteralKit.schema.ts:720-728` exports callable overloads for `LiteralKit(literals, enumMapping?)`, and `rg -n "LiteralKit\\.make" packages --glob '**/*.{ts,tsx}'` returned no matches. **Fix required:** replace every `LiteralKit.make(...)` recommendation with the real `LiteralKit([...], mappings?)` API, or identify an existing mapped-literal helper if that is what is intended. Add one current in-repo example to the research so implementation does not start from a nonexistent API.

2. **Claim (`RESEARCH.md:214-221`; `research/span-gated-resolver-algorithm-in-effect.md:75`): "`spanFromMatch(start, matchedText)` builds a `Contract.Span`" and "reuse `spanFromMatch` directly."** → The helper exists but is private, so the reuse path is false as written. `packages/foundation/capability/langextract/src/Alignment/index.ts:71` declares `const spanFromMatch = ...`; `rg -n "spanFromMatch|export .*spanFromMatch" packages/foundation/capability/langextract/src packages/foundation/capability/langextract/src/index.ts` found only local uses and no export. **Fix required:** either promote/export a documented span helper from `@beep/langextract`, or state that the court resolver must construct `Contract.Span` itself via the public `@beep/nlp/Handoff/Contract` API.

3. **Claim (`research/court-jurisdiction-reporter-vocabulary-schema.md:55`): the crosswalk codec should be "implemented as a `S.transformOrFail` backed by a static HashMap."** → This is stale for the repo's current Effect v4 beta. `package.json:157` pins `effect` to `4.0.0-beta.91`; current source exposes `SchemaTransformation.transformOrFail` and `SchemaGetter.transformOrFail` (`node_modules/effect/src/SchemaTransformation.ts:286`, `node_modules/effect/src/SchemaGetter.ts:538`), and in-repo code uses those forms. `rg -n "S\\.transformOrFail|Schema\\.transformOrFail" packages node_modules/effect/src` surfaced only an internal docs comment for `Schema.transformOrFail`, not a usable `S.transformOrFail` helper. **Fix required:** rewrite the schema recommendation to the current v4 style, cite one checked-in example such as `packages/foundation/modeling/schema/src/Xml.ts` or `packages/shared/domain/src/values/LocalDate/LocalDate.behavior.ts`, and specify how typed decode failures are represented.

4. **Claim (`RESEARCH.md:63-65`, `RESEARCH.md:346-347`; `research/court-reporter-citation-crosswalk.md:10`): reporters-db is "v3.2.41 (2024-02-09)" with "1,167 reporters, 2,102 variations."** → This version/count evidence is stale in a document dated 2026-06-29. PyPI currently lists `reporters-db` `3.2.66`, released 2026-06-25 (<https://pypi.org/project/reporters-db/>). The doc says to re-pin later, but the synthesis still presents old counts as if they support current design decisions. **Fix required:** update the current-version statement and recompute counts from the pinned tag/commit, or explicitly label the v3.2.41 counts as historical examples that must not feed MAP/SPEC acceptance criteria.

5. **Claim (`RESEARCH.md:230-239`, `RESEARCH.md:286-291`; `research/court-jurisdiction-reporter-vocabulary-schema.md:61-62`): SKOS terms already exist and the LiteralKit-to-SKOS emitter can emit "`notation`=code."** → The broad SKOS inventory is mostly right, but the specific predicate the proposed emitter needs is missing. `packages/foundation/modeling/rdf/src/Vocab/Skos.ts:32-184` exports Concept, ConceptScheme, label, broader/narrower/related, inScheme, topConcept, and match constants; `rg -n "SKOS_NOTATION|notation" packages/foundation/modeling/rdf/src packages/foundation/capability/semantic-web/src --glob '**/*.{ts,tsx}'` found no `SKOS_NOTATION`. **Fix required:** add `SKOS_NOTATION` to `@beep/rdf/Vocab/Skos` or state that the emitter will create the predicate with `makeNamedNode`, and add tests that prove code values are serialized with `skos:notation`.

## Advisory

1. **Claim (`research/court-jurisdiction-reporter-vocabulary-schema.md:62`): "`@beep/rdf` + `@beep/semantic-web` (`packages/foundation/capability/{rdf,semantic-web}`)" exist at that path.** → The subfile preserves a false path even though `RESEARCH.md:237-239` corrects it. `find packages -path '*/package.json' ...` confirms `@beep/rdf` is `packages/foundation/modeling/rdf`, and `ls packages/foundation/capability/rdf` fails while `packages/foundation/capability/semantic-web` exists. Suggested improvement: update the subfile so raw research does not keep contradicting the synthesis.

2. **Claim (`RESEARCH.md:272-277`; `research/official-data-sync-ingestion-contract.md:75-76`): courts-db render-then-parse is an "engine-deviation risk" and "`internal/Source.ts` helper surface [was] not read in full."** → The risk is overstated after repo inspection. `packages/tooling/tool/cli/src/commands/SyncDataToTs/internal/Source.ts:54-60` exposes both `bytes` and `text`; `Source.ts:163-183` fetches bytes, hashes them, and decodes text; `targets/IanaTimezones.ts:240-242` already consumes raw bytes before parsing. Suggested improvement: reframe this as a courts-specific target-design task, not an unverified engine capability gap. The plan should say "render inline in `acquire` from `source.text` and test the rendered JSON" unless a reusable helper is intentionally added.

3. **Claim (`RESEARCH.md:102-115`): the courts-db resolver pipeline is "verbatim two-source verified."** → The subfile does not support that confidence level. `research/span-gated-resolver-algorithm-in-effect.md:100-104` says the exact court-ID cardinality, `gather_regexes` tuple order, `strip_punc` character class, and RE2 feature coverage remain unverified or not byte-for-byte. Suggested improvement: downgrade the synthesis to "partially verified; pin and re-read exact source before porting," or add pinned commit excerpts for the exact loop, tuple shape, flags, and punctuation normalization.

4. **Claim (`RESEARCH.md:122-136`; `research/span-gated-resolver-algorithm-in-effect.md:62-68`): use `fuzzball.js`, `re2js`/`node-re2`, and audit/conversion tools (`safe-regex`, `recheck`, `pyre-to-regexp`, `js-regex`) as candidate mitigations.** → The license/maintenance gate is incomplete. `rg -n "fuzzball|re2js|node-re2|safe-regex|recheck|pyre-to-regexp|js-regex" package.json bun.lock packages --glob '**/*.{ts,tsx}'` found no checked-in dependency or prior package decision for these tools, and only `fuzzball.js` is given a license in the research. Suggested improvement: add a dependency table with package name, version, license, native/Bun compatibility, maintenance status, and whether the tool is runtime, dev-only, or offline validation.

5. **Claim (`RESEARCH.md:329-330`; `research/courts-db-license-and-rederivation.md:40`): BSD-2 attribution is discharged by one "`THIRD_PARTY_NOTICES` entry."** → The attribution target is not grounded in this repo. `rg -n "THIRD_PARTY_NOTICES|NOTICE" . --glob '!node_modules/**' --glob '!bun.lock'` found no root `THIRD_PARTY_NOTICES`; only unrelated plugin licenses, other exploration discussions, and a package-local HTML `NOTICE` reference surfaced. Suggested improvement: identify the actual repo licensing convention or explicitly create the new attribution file/path in MAP acceptance criteria.

6. **Claim (`research/court-jurisdiction-reporter-vocabulary-schema.md:56`): court ids and reporter keys should be EntityId-style schemas; "Source: repo memory / `schema-model-specialist` skill; CLAUDE.md schema-first rules."** → The design direction is plausible, but the citation is not reproducible research evidence. A future implementer needs concrete source files, not memory/skill references. Suggested improvement: cite the actual identity composer / `EntityId.factory` source and name the destination package for court ids, or mark this as an architecture decision still needing repo-owner alignment.

7. **Claim (`RESEARCH.md:286-291`): the LiteralKit-to-SKOS emitter is net-new because no function maps a LiteralKit member set to SKOS triples.** → This is true at the mapping level, but the research underuses existing RDF constructors. `packages/foundation/modeling/rdf/src/Rdf.ts:794`, `:883`, `:945`, and `:981` already provide `makeNamedNode`, `makeLiteral`, `makeQuad`, and `makeDataset`, and `@beep/semantic-web/rdf` re-exports RDF substrate. Suggested improvement: scope the gap as "new mapping function only"; require reuse of the existing quad/dataset constructors and the existing SKOS constants, plus the missing `SKOS_NOTATION` predicate from the blocking finding.

## Confirmed sound

- `@beep/courtlistener` is a bare driver stub: `ls packages/drivers/courtlistener/src` shows only `index.ts`, and `packages/drivers/courtlistener/src/index.ts:1-11` exports only `VERSION`.
- `@beep/data` has a generated landing zone but no courts dataset: `ls packages/foundation/primitive/data/src/generated` shows `cldr-territories`, `iana-media-types`, `iana-timezones`, and `iso4217` only.
- The sync engine and target registry exist: `SyncDataTargetServices = HttpClient | Crypto` and the four-field `SyncDataTarget` contract are in `packages/tooling/tool/cli/src/commands/SyncDataToTs/internal/Models.ts:130-143`; `targets/index.ts:24-29` registers only the four current targets.
- The controlled court/jurisdiction/reporter vocabulary is genuinely absent from current package source. `rg -n "CourtJurisdictionCode|ReporterType|citation_string|CourtSystem|CourtLevel|CourtType|Reporter|Jurisdiction|Court" packages/law-practice/domain/src packages/drivers/courtlistener/src packages/foundation/primitive/data/src --glob '**/*.{ts,tsx}'` returned no matches.
- `@beep/langextract` and `@beep/nlp` provide useful substrate: `GroundedExtraction` and `AlignmentStatus` are in `packages/foundation/capability/langextract/src/Extraction/index.ts:118` and `:211`; the span model is half-open `[start,end)` in `packages/foundation/capability/nlp/src/Handoff/Contract.ts:162-178`; the current similarity helper is Levenshtein, not token-sort (`packages/foundation/capability/langextract/src/Alignment/index.ts:132-156`).
- No existing court resolver, token-sort fuzzy matcher, or RE2-backed regex path was found in `packages/`: `rg -n "find_court|gather_regexes|reduce_court_list|token_sort_ratio|fuzzball|re2js|node-re2|safe-regex|recheck" packages --glob '**/*.{ts,tsx}'` returned no matches.
- The synthesis correction that `@beep/rdf` lives at `packages/foundation/modeling/rdf` is sound, and `@beep/semantic-web` exists at `packages/foundation/capability/semantic-web`.
- `goals/ip-law-knowledge-graph` already owns abstract Court/Jurisdiction graph concepts: `p1-schema-design.md` contains `Jurisdiction`, `Court`, `DecidedBy`, `GovernedBy`, and "OWL Class Mapping" entries.
- The courts-db version claim held up: PyPI currently lists `courts-db` `0.10.27` from 2026-03-25 (<https://pypi.org/project/courts-db/>).

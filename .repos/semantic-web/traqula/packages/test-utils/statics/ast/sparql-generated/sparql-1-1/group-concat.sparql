SELECT ?s ( GROUP_CONCAT( ?p ;SEPARATOR=" " ) AS ?plist ) ( GROUP_CONCAT( DISTINCT ?p ;SEPARATOR=",\"/" ) AS ?plist2 ) ( GROUP_CONCAT( DISTINCT ?p ;SEPARATOR="" ) AS ?plist3 ) WHERE {
  ?s ?p ?c .
}
GROUP BY ?s
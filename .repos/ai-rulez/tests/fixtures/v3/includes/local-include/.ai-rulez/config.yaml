version: "3.0"
name: base-project-with-local-include
description: Base project that includes local shared rules
presets:
  - claude
includes:
  - name: shared-rules
    source: ./shared-rules
    include:
      - rules
      - context
    merge_strategy: local-override

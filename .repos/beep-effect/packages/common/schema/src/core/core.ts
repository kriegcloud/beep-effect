/**
 * Core marker module to satisfy documentation coverage.
 *
 * Provides a documented anchor for the core namespace while concrete helpers live in sibling modules.
 *
 * @category Core
 * @since 0.1.0
 */
export const coreModule = true;

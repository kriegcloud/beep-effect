/**
 * Re-exports the full `Contract` namespace from the internal implementation so
 * consumers can import `@beep/contract/Contract` without reaching into
 * `src/internal`.
 *
 * @see ./internal/contract
 */
export * from "./internal/contract";

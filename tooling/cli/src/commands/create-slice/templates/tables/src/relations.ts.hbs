/**
 * {{SliceName}} table relations
 *
 * Defines Drizzle relations between tables in this slice.
 *
 * @module {{sliceName}}-tables/relations
 * @since 0.1.0
 */
import { relations } from "drizzle-orm";

// Import tables when created
// Example: import { myEntityTable } from "./tables/my-entity.table.js";

// Define relations here
// export const myEntityRelations = relations(myEntityTable, ({ one, many }) => ({
//   // Define foreign key relationships
// }));

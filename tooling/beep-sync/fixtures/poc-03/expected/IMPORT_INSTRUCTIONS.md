# JetBrains Prompt Library Import

1. Treat this directory as managed output from `beep-sync`.
2. Import prompts from `prompts.md` via JetBrains Prompt Library UI.
3. Keep `prompts.json` as deterministic machine-readable sidecar.

Do not hand-edit generated artifacts.

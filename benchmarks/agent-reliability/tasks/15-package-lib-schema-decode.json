{
  "id": "package_lib_03",
  "title": "Schema decode migration correctness",
  "category": "package_lib",
  "prompt": "Replace deprecated Schema.decode usage with decodeUnknownSync or decodeUnknownEffect where appropriate.",
  "cwd": "packages/common/schema",
  "acceptanceCommands": ["bun run check"],
  "timeoutMinutes": 8,
  "expectedTop5Targets": [
    "Schema.decode is removed in v4. Use Schema.decodeUnknownSync or Schema.decodeUnknownEffect."
  ],
  "kgContextReviewerScores": [
    { "reviewerId": "panel_a", "score": 4.5 },
    { "reviewerId": "panel_b", "score": 4.4 }
  ],
  "touchedPathAllowlist": ["packages/common/schema/src"]
}

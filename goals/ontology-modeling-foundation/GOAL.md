# Goal

Build a foundation-grade ontology builder POC where Effect Schema classes carry
ontology metadata through annotations, `@beep/identity` removes repetitive
identifier work, `@beep/rdf` provides RDF-safe values, and `@beep/ontology`
assembles annotated schemas into JSON-LD and Turtle.

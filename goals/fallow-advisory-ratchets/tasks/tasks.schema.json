{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "beep/fallow-advisory-ratchets/tasks.schema.json",
  "title": "Fallow Advisory Ratchets Tasks",
  "description": "Task inventory for the fallow-advisory-ratchets goal. The Effect Schema in goals/fallow-advisory-ratchets/ops/validate-packet.ts is the authority; this companion exists for editor tooling.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "schemaVersion",
    "updated",
    "validatorVersion",
    "tasks"
  ],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": "fallow-advisory-ratchets/tasks/v1" },
    "updated": { "type": "string" },
    "validatorVersion": { "const": "fallow-advisory-ratchets-validator/v1" },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "rank",
          "status",
          "phase",
          "title",
          "summary",
          "implementationScope",
          "proofCommands",
          "acceptanceCommands",
          "dependencies",
          "rollbackPlan",
          "evidenceRefs",
          "owner",
          "riskLevel",
          "decisionGate"
        ],
        "properties": {
          "id": { "type": "string", "pattern": "^far-[0-9]{3}$" },
          "rank": { "type": "integer", "minimum": 1 },
          "status": {
            "enum": [
              "seeded",
              "selected",
              "in-progress",
              "done",
              "deferred",
              "rejected",
              "blocked"
            ]
          },
          "phase": { "enum": ["P0", "P1", "P2", "P3", "P4"] },
          "title": { "type": "string", "minLength": 1 },
          "summary": { "type": "string", "minLength": 1 },
          "implementationScope": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "proofCommands": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "acceptanceCommands": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "dependencies": {
            "type": "array",
            "items": { "type": "string", "pattern": "^far-[0-9]{3}$" }
          },
          "rollbackPlan": { "type": "string", "minLength": 1 },
          "evidenceRefs": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "owner": { "type": "string", "pattern": "^@" },
          "riskLevel": { "enum": ["low", "medium", "high"] },
          "decisionGate": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "status", "evidence"],
            "properties": {
              "kind": {
                "enum": [
                  "packet-authored",
                  "policy-inventory",
                  "ratchet-candidate",
                  "promotion-readiness",
                  "deferred",
                  "none"
                ]
              },
              "status": { "enum": ["open", "blocked", "passed", "failed"] },
              "evidence": {
                "type": "array",
                "minItems": 1,
                "items": { "type": "string", "minLength": 1 }
              }
            }
          }
        }
      }
    }
  },
  "x-effectSchemaAuthority": "goals/fallow-advisory-ratchets/ops/validate-packet.ts"
}

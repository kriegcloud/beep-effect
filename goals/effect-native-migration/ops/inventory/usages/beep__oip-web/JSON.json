[
  {
    "category": "JSON",
    "package": "@beep/oip-web",
    "filePath": "apps/oip-web/src/app/page.tsx",
    "line": 22,
    "nativeSnippet": "const safeJsonScript = (value: unknown) => Str.replaceAll(\"<\", \"\\\\u003c\")(JSON.stringify(value));",
    "nativeSymbol": "JSON.stringify",
    "suggestedTarget": "S.UnknownFromJsonString (encode side: S.encodeUnknownEffect)",
    "suggestedImport": "import * as S from \"effect/Schema\"",
    "needsHumanDecision": true,
    "note": "Receiver is `unknown` (JSON-LD graph value injected into a <script> tag); no target schema exists. Use S.UnknownFromJsonString encode (S.encodeUnknownEffect) as the safe fallback, or model a JSON-LD schema later. The trailing Str.replaceAll('<', '\\\\u003c') is already Effect-native and stays. S import (effect/Schema) already present in this file; JSON is a no-wrapper category -> raw effect/Schema regardless of canImportUtils:true."
  }
]

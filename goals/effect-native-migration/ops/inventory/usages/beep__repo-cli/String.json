[
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/src/commands/Docgen/internal/QualityWorkerEval.ts",
    "line": 825,
    "nativeSnippet": "trimmed.replace(/\\/+$/, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "`trimmed` is a string (result of Str.trim(value)) in ossProviderBaseUrl; strips trailing slashes via a RegExp. Replace with Str.replace(/\\/+$/, \"\")(trimmed) (Str.replace accepts RegExp)."
  },
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/src/commands/Quality/ScriptCommands.ts",
    "line": 751,
    "nativeSnippet": "readme.slice(tableStart + effectTsgoDiagnosticsTableStartMarker.length, tableEnd)",
    "nativeSymbol": "String.prototype.slice",
    "suggestedTarget": "Str.slice",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "`readme` is a string parameter (extractEffectTsgoDiagnosticsTableFragment). Substring extraction with computed start/end offsets. Replace with Str.slice(start, tableEnd)(readme). Note adjacent readme.indexOf(...) on lines 746-747 is String.prototype.indexOf, which is not in the targeted String-method set, so not flagged."
  },
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/test/repo-exports-catalog.test.ts",
    "line": 8,
    "nativeSnippet": "[base.replace(/\\/+$/u, \"\"), ...segments.map((segment) => segment.replace(/^\\/+|\\/+$/gu, \"\"))]",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Local joinPath helper in a test file: `base` and `segment` are strings, each .replace strips slashes via RegExp. In scope per SPEC §2 (String ban applies to tests). Two native .replace calls on this line (base.replace and segment.replace). The trailing .filter/.join are Array methods, out of scope for tests."
  },
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/test/foundation-topology.test.ts",
    "line": 6,
    "nativeSnippet": "fileURLToPath(new URL(\"../../../../..\", import.meta.url)).replace(/\\/$/u, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "`fileURLToPath(...)` returns a string; .replace strips a trailing slash via RegExp. In scope per SPEC §2 (String ban applies to tests). This file already imports { A, Str } from \"@beep/utils\", so Str.replace is directly available."
  },
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/test/foundation-topology.test.ts",
    "line": 8,
    "nativeSnippet": "[base.replace(/\\/+$/u, \"\"), ...segments.map((segment) => segment.replace(/^\\/+|\\/+$/gu, \"\"))]",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Local joinPath helper: `base` and `segment` are strings. Two native .replace calls on this line. In scope per SPEC §2. File already imports Str from \"@beep/utils\"."
  },
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/test/architecture-operation-plan.test.ts",
    "line": 32,
    "nativeSnippet": "[base.replace(/\\/+$/u, \"\"), ...segments.map((segment) => segment.replace(/^\\/+|\\/+$/gu, \"\"))]",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Local joinPath helper: `base` and `segment` are strings. Two native .replace calls on this line. In scope per SPEC §2 (String ban applies to tests)."
  },
  {
    "category": "String",
    "package": "@beep/repo-cli",
    "filePath": "packages/tooling/tool/cli/test/jsdoc-categories.test.ts",
    "line": 67,
    "nativeSnippet": "\"A\".repeat(10_000)",
    "nativeSymbol": "String.prototype.repeat",
    "suggestedTarget": "Str.repeat",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Native String.prototype.repeat on a string literal to build an overlong category fixture. In scope per SPEC §2. Replace with Str.repeat(10_000)(\"A\")."
  }
]

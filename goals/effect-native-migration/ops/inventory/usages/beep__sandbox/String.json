[
  {
    "category": "String",
    "package": "@beep/sandbox",
    "filePath": "packages/foundation/capability/sandbox/test/sandbox.test.ts",
    "line": 60,
    "nativeSnippet": "base.replace(/\\/+$/u, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Native `.replace` on the `base` string inside the test-local `joinPath` helper. SPEC §2 puts the String ban in scope for test files. Replace with `Str.replace(/\\/+$/u, \"\")(base)` (or a `pipe`/`flow` form). Identical helper appears in `parity-helpers.test.ts`."
  },
  {
    "category": "String",
    "package": "@beep/sandbox",
    "filePath": "packages/foundation/capability/sandbox/test/sandbox.test.ts",
    "line": 60,
    "nativeSnippet": "segment.replace(/^\\/+|\\/+$/gu, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Native `.replace` on the `segment` string inside the `segments.map((segment) => ...)` callback of the test-local `joinPath` helper. Replace with `Str.replace(/^\\/+|\\/+$/gu, \"\")`. The surrounding `.map`/`.filter`/`.join` are Array methods which are out of scope in test files per SPEC §2 (only JSON+String are in scope for tests)."
  },
  {
    "category": "String",
    "package": "@beep/sandbox",
    "filePath": "packages/foundation/capability/sandbox/test/parity-helpers.test.ts",
    "line": 26,
    "nativeSnippet": "base.replace(/\\/+$/u, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Native `.replace` on the `base` string inside the test-local `joinPath` helper (duplicate of the helper in `sandbox.test.ts`). SPEC §2 puts the String ban in scope for test files. Replace with `Str.replace(/\\/+$/u, \"\")(base)`."
  },
  {
    "category": "String",
    "package": "@beep/sandbox",
    "filePath": "packages/foundation/capability/sandbox/test/parity-helpers.test.ts",
    "line": 26,
    "nativeSnippet": "segment.replace(/^\\/+|\\/+$/gu, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import { Str } from \"@beep/utils\"",
    "note": "Native `.replace` on the `segment` string inside the `segments.map((segment) => ...)` callback of the test-local `joinPath` helper. Replace with `Str.replace(/^\\/+|\\/+$/gu, \"\")`. Surrounding `.map`/`.filter`/`.join` are Array methods, out of scope in tests per SPEC §2."
  }
]

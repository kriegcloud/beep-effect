[
  {
    "category": "String",
    "package": "@beep/utils",
    "filePath": "packages/foundation/modeling/utils/test/Glob.test.ts",
    "line": 33,
    "nativeSnippet": "base.replace(/\\/+$/u, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import * as Str from \"effect/String\"",
    "note": "Test file (String is in-scope for tests). `base` is a string param of joinPath. canImportUtils:false -> raw effect/String. `Str.replace(searchValue, replacement)(self)`."
  },
  {
    "category": "String",
    "package": "@beep/utils",
    "filePath": "packages/foundation/modeling/utils/test/Glob.test.ts",
    "line": 33,
    "nativeSnippet": "segment.replace(/^\\/+|\\/+$/gu, \"\")",
    "nativeSymbol": "String.prototype.replace",
    "suggestedTarget": "Str.replace",
    "suggestedImport": "import * as Str from \"effect/String\"",
    "note": "Test file. `segment` is a string inside segments.map(...). canImportUtils:false -> raw effect/String. Global regex; `Str.replace` accepts a RegExp searchValue. Note: outer `segments.map` is Array category and out-of-scope for test files (only JSON/String flagged in tests)."
  }
]

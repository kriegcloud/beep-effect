# Yeet Lane Research Prompt

Inspect the current Yeet command planner, handler, packet renderer, tests, and
repo-run proof definitions. Find the minimal design for:

- `verify --tier review-fix`
- `publish --amend --no-edit --reuse-verified`
- durable run state
- new `closeout` mode

Preserve full publish proof and current publish safety checks.

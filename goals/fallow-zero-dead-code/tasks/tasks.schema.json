{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "beep/fallow-zero-dead-code/tasks.schema.json",
  "title": "Fallow Zero Dead-Code Tasks",
  "description": "Task inventory for the fallow-zero-dead-code goal. The Effect Schema in goals/fallow-zero-dead-code/ops/validate-packet.ts is the authority; this companion exists for editor tooling.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "schemaVersion",
    "updated",
    "validatorVersion",
    "tasks"
  ],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": "fallow-zero-dead-code/tasks/v1" },
    "updated": { "type": "string" },
    "validatorVersion": { "const": "fallow-zero-dead-code-validator/v1" },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "rank",
          "status",
          "phase",
          "title",
          "summary",
          "implementationScope",
          "proofCommands",
          "acceptanceCommands",
          "dependencies",
          "rollbackPlan",
          "evidenceRefs",
          "owner",
          "riskLevel",
          "decisionGate"
        ],
        "properties": {
          "id": { "type": "string", "pattern": "^fzd-[0-9]{3}$" },
          "rank": { "type": "integer", "minimum": 1 },
          "status": {
            "enum": [
              "seeded",
              "selected",
              "in-progress",
              "done",
              "deferred",
              "rejected",
              "blocked"
            ]
          },
          "phase": { "enum": ["P0", "P1", "P2", "P3", "P4"] },
          "title": { "type": "string", "minLength": 1 },
          "summary": { "type": "string", "minLength": 1 },
          "implementationScope": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "proofCommands": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "acceptanceCommands": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "dependencies": {
            "type": "array",
            "items": { "type": "string", "pattern": "^fzd-[0-9]{3}$" }
          },
          "rollbackPlan": { "type": "string", "minLength": 1 },
          "evidenceRefs": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "owner": { "type": "string", "pattern": "^@" },
          "riskLevel": { "enum": ["low", "medium", "high"] },
          "decisionGate": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "status", "evidence"],
            "properties": {
              "kind": {
                "enum": [
                  "research-complete",
                  "zero-findings",
                  "feature-matrix-promotion",
                  "merge-readiness",
                  "none"
                ]
              },
              "status": { "enum": ["open", "blocked", "passed", "failed"] },
              "evidence": {
                "type": "array",
                "minItems": 1,
                "items": { "type": "string", "minLength": 1 }
              }
            }
          }
        }
      }
    }
  }
}
